# Baseline e2e

Зафиксированное состояние кейсов. Обновляется только явной командой `--update-baseline`.
Статусы `ERROR` и провалы teardown здесь не фиксируются.

## Чего в baseline нет и почему

Отсутствие строки — это «не прогонялось», а не «зелено». Профиль `astra-vm`
снят на 57 кейсах из 82, и шесть suite'ов в нём не участвовали. Полный прогон
профиля вернёт их разом, и без этой записи он читался бы как лавина регрессий:

- **INST (5), `10-install`** — идемпотентного teardown по SSH нет. `postrm`
  снимает только `@include tessera*`, оставляя `session required pam_tessera.so`
  на удалённый модуль, и машина теряет sudo и вход; пересоздать её нельзя.
  Записано в `openspec/changes/e2e-harness/tasks.md`, задачи 7.1 и 7.1a.
- **ISS (7), `60-issuer`** и **SIGN (4), `61-issuer-signing`** — подготовка
  `deploy-issuer` требует бинарь `issuer`, который раннер везёт в окружение как
  артефакт стенда (`[[artifacts]]` в `stand.toml`). В прогоне 2026-08-10 он не
  объявлялся: «помимо пакета в окружение ничего не везлось»
  (`runs/2026-08-10-astra-vm-af35b94/report.md`). Подготовка отдала бы `ERROR`.
- **TOK (4), `70-token-carrier`** — все кейсы требуют `hardware-token`, а
  профиль его намеренно не объявляет: USB-проброс в эту виртуальную машину не
  настроен (`profiles/astra-vm.toml`). Прогон дал бы `SKIP`, а не проверку.
- **MON (2), `23-monitord`** — подготовка `start-monitord` написана под
  окружение без D-Bus: она правит штатный юнит drop-in'ом `--no-dbus`, отключая
  ровно тот путь через logind, ради которого профиль и нужен, а снять drop-in
  на живой машине нечем — teardown у suite'ов нет. Отдельной записи о причине
  пропуска в репозитории нет, это восстановление по коду подготовки.
- **PAMI (3), `17-pam-integration`** — не прогонялся, причина не записана.
  Кейсы работают на временных файлах в `/tmp` и живой PAM-стек не трогают;
  препятствий, видимых по репозиторию, нет.
- **CLI-004…CLI-010 (7), `12-cli-diagnostics`** — написаны вместе с изменением
  `gost-engine-preload`, прогона ещё не было. Прогонять их на `.deb` из `main`
  бессмысленно как проверка: ГОСТ-шага префлайта там пока нет, поэтому
  CLI-004/005/006 на нём заведомо красные — это ожидаемое состояние «кейс
  написан раньше кода», а не регрессия. CLI-007 закрывает второй, уже
  существующий барьер (валидатор пути) и на `main` должен быть зелёным;
  расхождение здесь означало бы, что кейс воспроизводит не то. CLI-006 и
  CLI-008 требуют `gost-engine` и вне `astra-vm` дают `SKIP`. Первый
  осмысленный прогон — на сборке с этим изменением; до него ни одна из семи
  строк в таблицу ниже не попадает.

- **CLI-010 добавлен позже остальных** — по находке независимого ревью уже
  после первой правки шага: запись о неверной паре `mode`/`crypto_backend`
  выдавалась только когда разрешён ГОСТ, то есть проверка режима зависела от
  настройки ГОСТа, и обычная RSA/ECDSA-конфигурация с той же неработающей
  парой проходила префлайт молча. Кейс не прогонялся ни разу; на `.deb` из
  `main` он заведомо красный, потому что записи `pkcs11_openssl_unsupported`
  там ещё нет.

- **CLI-005 переписан по итогам ревью PR #94.** Прежнее ожидание — `exit 0` и
  `[WARN ] gost_engine_configured_unused` — закрепляло дефект: режим хелпера
  `gost-engine-unused` подставляет заведомо неисправный движок, а
  аутентификация грузит его по самому факту заданного пути, так что на такой
  машине не входит никто. Теперь кейс ждёт `[ERROR] gost_engine_load_failed` и
  `exit 1`. Прежнюю ветку (движок исправен, ГОСТ-подписи не разрешены →
  предупреждение и `exit 0`) проверяет новый CLI-008 на настоящем движке.

- **CLI-008 и CLI-009 опираются на новые режимы `config-mutate.sh`** —
  `gost-engine-unused-real` и `gost-pkcs11-openssl`. До первого прогона на
  стенде сами режимы тоже не проверены на живой системе: локально сверен
  только результат подстановки на `dist/config/config.toml.example`.

- **Изменение привилегированной загрузки конфигурации в CLI затрагивает весь
  реестр.** `tessera daemon`, `tessera check` и `tessera enroll` перешли на
  `load_privileged_validated_config`, поэтому на стенде, где `config.toml`,
  якоря, CRL, `pkcs11_module` или `gost_engine_path` лежат не под root или
  доступны на запись остальным, красными станут не только ГОСТ-кейсы, а все
  кейсы, которые дёргают эти команды. Такой результат — сигнал о правах на
  стенде, а не о дефекте продукта; проверять `sudo tessera check` до прогона.
- **CODE-014, CODE-015, CODE-016 (3), `27-codes-phone`** — заведены 2026-08-21 при
  переработке под серверную выдачу (R.1-R.3) и не прогонялись ни разу по той же причине,
  что и весь suite: в main этого кода нет. Что каждый стережёт:
  - **CODE-014** — код, посчитанный из одного статического ключа устройства, не пускает.
    Это модель снятого диска или бэкапа; до R.1 такой код сходился, потому что из этого
    ключа и выводился общий ключ. Написан ДО реализации, как требует правило.
  - **CODE-015** — код, вычисленный для одного личного номера инженера, не сходится при
    другом. Написан ДО реализации R.2.
  - **CODE-016** — однажды сработавший код не открывает вход второй раз, и перезапуск
    этого не меняет. Пришёл на смену CODE-013 (откат копии счётчика), удалённому вместе с
    персистом счётчика в R.3. **Написан ПОСЛЕ кода** — правило «кейс раньше кода» на нём
    не соблюдено; отмечено здесь, потому что кейс, появившийся после реализации, стережёт
    ровно то, что реализация уже делает, и его красный прогон ничего не опроверг бы.

- **CODE-014 после R.10 требует от стенда ещё один артефакт.** Подпись challenge ключом
  устройства сделала прежний ввод непроходимым: хелпер подменял эфемерную точку и отдавал
  строку выдаче, а подписанная строка после подмены не проверяется. Кейс сохранён в
  прежнем виде — отказ обязан приходить из сверки кода, а не из подписи, потому что у
  снявшего диск ключ устройства есть по условию задачи и он подписал бы законно. Поэтому
  стенд подписывает подменённый challenge заново, инструментом раннера
  (`xtask codes-sign-challenge`), который берёт канонические байты у крейта контракта — не
  собирая их сам, иначе кейс проверял бы совпадение двух написаний формата.
  Что нужно доставить: **бинарь раннера** секцией `[[artifacts]]` в `stand.toml` (по
  умолчанию ожидается как `tessera-xtask` в PATH, переопределяется `TESSERA_E2E_XTASK`) и
  файл `device-key.pem` в комплекте фикстур — он появляется у `cargo xtask codes-fixtures`,
  комплект нужно перевыпустить. Пока бинаря нет, кейс падает как СБОЙ СТЕНДА с названной
  причиной, а не как отказ продукта.

- **CODE-015 после R.10 стережёт более ранний рубеж.** Подмена личного номера ломает
  подпись устройства, поэтому выдача отказывает раньше, чем что-нибудь посчитает: кейс
  переписан на отказ выдачи с классом `challenge_signature_rejected`. Вторая половина той
  же гарантии — что личный номер входит в БАЙТЫ кода и код на одного не сходится у
  другого — снаружи больше не наблюдается по той же причине, по которой не наблюдается
  CODE-014, и держится юнит-тестами ядра.

- **Гарантий гранта в e2e НЕТ, и это долг с названной причиной.** При R.4 заведены
  форматы подписанного запроса инженера, гранта и status-token. Двух кейсов, которые
  напрашиваются — «грант с разошедшимися продублированными полями отвергается» и
  «повторный грант на тот же nonce не принимается», — здесь нет намеренно: **их некому
  исполнить**. На устройстве нет потребителя гранта (кабельный транспорт D21 не
  реализован: PAM-ветка знает только MAC-код), а выпускающей стороны — сервера,
  подписывающего грант, — в репозитории нет вовсе. Кейс, красный по причине «механизма
  не существует», ничего не стережёт: в реестре красный обязан означать дефект, а не
  отсутствие мира вокруг. До прихода транспорта гарантии держат контрактные тесты с
  мутационной проверкой (`crates/tessera_codes_contract/src/{request,grant,status}.rs`):
  побайтовое равенство сводки гранта по каждому классу расхождения, разделение подписей
  метками, привязка status-token к nonce и digest'у авторизации. Кейсы заводятся вместе
  с кабельным транспортом.

  То же относится к документам инженера (запись реестра и авторизация, R.6) и к записи
  реестра устройства в её новом виде (R.5): их читает либо только выдача, либо никто —
  потребитель авторизации это сервер и кабельный транспорт. Гарантии держат контрактные
  тесты с мутацией; кейсы едут вместе с транспортом.

- **CODE (13), `27-codes-phone`** — suite заведён 2026-08-17 вместе со спекой телефонного
  канала. К вечеру того же дня готово всё, что от нас зависит: контрактный крейт,
  device-side ядро, PAM-ветка `method=code`, консольная выдача `issuer codes`, хелпер
  `codes-phone.sh`, режим построчных ответов в `pam-drive`, комплект фикстур
  (`cargo xtask codes-fixtures`). Красным suite держит **одна** причина: стенд ставит
  `.deb` из релизных артефактов main, а этого кода в main ещё нет; правило репозитория
  запрещает подменять пакет локальной сборкой — тогда проверялся бы не тот код.
  Первый осмысленный прогон возможен после мержа.
  Что известно заранее про отдельные кейсы:
  - CODE-001 и CODE-006 требуют `mac`: уровень читается из метки процесса, и на
    контейнерном профиле кейс про уровень «прошёл» бы, ничего не проверив;
  - CODE-004 ждёт `PAM_MAXTRIES` (11) — путь к нему проверен unit-тестами против живого
    метода (`codes_flow/live_tests.rs`, бюджеты 2/3/5), так что красный тут будет означать
    поломку стенда, а не известный разрыв;
  - CODE-005 **переименован 2026-08-18** («Записанный код не пускает во второй раз, и
    перезапуск этого не меняет» вместо «Ранее принятый код не пускает повторно после
    перезапуска»). Прежний заголовок обещал одноразовость потреблённого nonce, а кейс её
    не проверял и проверить не может: половину nonce составляет случайный хвост, который
    устройство тянет на каждый challenge (`codes/tail.rs`), challenge печатает только
    само устройство, поэтому второго разговора с тем же nonce не бывает ни при каком
    внешнем воздействии — старый код не сходится по MAC раньше всякой проверки
    потреблённости. Оснастку, удерживающую challenge между разговорами, писать не стали
    именно поэтому: она невозможна, а не трудна. Одноразовость через перезапуск и откат
    состояния держат unit-тесты `a_spent_nonce_stays_spent_across_a_reboot` и
    `a_counter_file_behind_the_state_is_a_rollback` (`codes/state.rs`), ссылки на них
    стоят в комментарии кейса. Кейс оставлен: он стережёт полный путь (выдача → вход →
    перезапуск → повтор) на настоящем, однажды сработавшем коде, но механика отказа у
    него общая с CODE-003, и на большее он теперь не претендует;
  - CODE-007 **сужен 2026-08-18**: заголовок обещал границу «отзыв» целиком, а хелпер
    дописывает номер в `tickets.revoked` — один путь снятия права из двух. Второй путь,
    сборка набора без билета, отзывом не является (модель угроз, §5.15) и вынесен в
    отдельный кейс ENRC-012;
  - CODE-011 **уточнён 2026-08-18**: шаг `expect_journal` искал общее событие
    `qr_code_login`, а срез журнала берётся за весь кейс и поток начинается с разговоров,
    которым challenge выдаётся, — строка появлялась бы и в прогоне, где лимит выдачи не
    сработал вовсе. Теперь ищется отказ по бюджету выдачи
    (`qr_code_login.*issuance_throttled`, константа `codes::audit::REASON_ISSUANCE_THROTTLED`).
    Кавычки вокруг значения поля ставит форматтер трассировки, поэтому регексп к ним не
    привязан; на живом прогоне это первое, что стоит сверить глазами;
  - CODE-006 проверяет отказ на стороне ВЫДАЧИ: код вне рамок билета инженеру не выдаётся
    вовсе. Отказ устройства на математически верный код вне рамок снаружи невоспроизводим
    санкционированным путём — для этого код должен посчитать инструмент, рамок не
    проверяющий, то есть скомпрометированный оператор.
  - CODE-011 и CODE-012 заведены 2026-08-17 под собственный rate-limit выдачи. Они
    красные по той же общей причине (кода нет в main) и, в отличие от остальных,
    по времени: обе гоняют поток разговоров, а CODE-012 ещё и выжидает окно выдачи
    целиком. Пара неразделима — CODE-011 проверяет, что поток запросов упирается в
    ВРЕМЕННЫЙ отказ вместо исчерпанного насмерть счётчика, CODE-012 проверяет, что
    отказ отпускает сам. Порознь первый закрепил бы лекарство, неотличимое от болезни.
  - CODE-013 **добавлен 2026-08-18** (устройство, восстановленное из копии, не пускает по
    коду вовсе) — по побочной находке при сужении CODE-005: откат персистированного
    состояния наблюдаем снаружи, а кейса на него не было ни одного. Красный по общей
    причине выше. Своё:
    - воспроизводится только ЧАСТИЧНЫЙ откат — счётчик позади состояния. Счётчик и
      состояние пишутся раздельно (счётчик первым), и отставший счётчик и есть то
      единственное, по чему устройство узнаёт о подмене. СОГЛАСОВАННЫЙ откат, где из копии
      вернулись оба файла разом, кейс не покрывает и покрыть не может: он не детектируется
      ничем, и модель угроз говорит это прямо. Граница записана в самом кейсе;
    - ожидание `PAM_SYSTEM_ERR` (4) снято с кода: `CodeLoginError::StateRollback` →
      `CodeFlowError::DeviceState` → `pam_code() == 4`, закреплено unit-тестом
      `a_device_that_cannot_run_the_method_is_not_a_failed_attempt`
      (`crates/pam_tessera/src/codes_flow/tests.rs`). Девятка (`PAM_AUTHINFO_UNAVAIL`)
      здесь была бы дефектом: это единственный отказ, который стек вправе переступить;
    - хелперу дописаны `snapshot-counter`, `restore-counter` и
      `authenticate-without-code`. Первые две падают, если счётчик не ушёл вперёд от
      копии, — откат, которого нет, зазеленел бы, ничего не проверив. Третья нужна
      потому, что при откате устройство отказывает ДО промпта кода: обычный
      `authenticate` упёрся бы в ненапечатанный challenge и отдал сбой стенда (70)
      вместо вердикта продукта;
    - **расхождение спеки и механизма, кейсом не закрытое**: сценарий спеки «Устройство
      восстановлено из снапшота» говорит про откат «вместе с consumed-state», то есть про
      согласованный снимок, а код ловит только рассогласованный. Кейс написан по механизму
      и честно называет границу; формулировка спеки — вопрос к владельцу change'а.
  Непроверенное ничем, кроме чтения кода (увидит первый прогон): что живой `pam_tessera`
  печатает challenge ровно в том виде, на который настроен разбор хелпера, и что живой
  `issuer` принимает собранную хелпером проводную форму.

- **AUD (6), `29-audit-chain`** — suite заведён 2026-08-17 вместе с change'ом `audit-chain`.
  Ожидания измерены на локальной сборке `tessera audit annotate|verify|status|attest`
  (коды возврата 0/1/2/3, строки `status: intact` / `status: broken` /
  `first invalid record: N` / `status: full`, `"op":"chain_break"` первой строкой новой
  цепочки, путь отказа `calls[0].derived_key`) — то есть это не догадки, а снятое
  поведение. Красным suite держит одна причина: стенд ставит `.deb` из релизных
  артефактов main, где подкоманды `tessera audit` ещё нет вовсе, поэтому каждый кейс
  упрётся в разбор аргументов clap. Первый осмысленный прогон возможен после мержа.
  Кейсы намеренно не требуют ни `mac`, ни `usb`, ни живого PAM-стека: журналу нужен
  только установленный пакет и временный каталог, и суживать их до `astra-vm` значило бы
  не проверять их нигде.
  Что известно заранее: `sed -i '2d'` в AUD-001 и AUD-004 — GNU-форма, на целевых
  профилях (ubuntu-container, astra-*) она и работает; `openssl ecparam` в AUD-004
  берётся из системного пакета, который на обоих профилях есть.
  AUD-006 стоит особняком: это единственный кейс сюиты, проверяющий не инструмент
  `tessera audit`, а само устройство — что секция `[audit]` доезжает до PAM-модуля и
  вход по коду ложится в цепочку. Он красный по двум причинам сразу (нет подкоманд
  `audit` в main и нет кода телефонного канала в main) и зависит от ключа
  `--with-audit-chain` в `codes-phone.sh`, которого в хелпере ещё нет — это записанный
  долг, а не оплошность. Кейс заведён именно потому, что юнит-тесты этот шов не держат:
  они зовут `install_from_config` напрямую, а снятие её вызова из `pam_sm_authenticate`
  поймает только прогон на живой системе — ровно так контур однажды и оказался мёртвым
  при зелёных тестах.

- **CODE (7), дополнение от 2026-08-17 вечера** — к причинам выше добавилась ещё одна,
  и она переживёт мерж кода в main: `[codes].key_password` убран (change
  `codes-device-artifacts`), устройство открывает свой контейнер БЕЗ пароля, а
  `helpers/codes-phone.sh` по-прежнему кладёт в хранилище `device.p12` из комплекта
  фикстур — контейнер, закрытый PIN'ом. Значит CODE-001…007 упадут на материале ключа,
  а не на проверяемой гарантии, пока хелпер не начнёт раскладывать ключ продуктовым
  импортом (`tessera enroll --codes-pin-file`, как `helpers/codes-enroll.sh`) либо пока
  `cargo xtask codes-fixtures` не начнёт класть рядом хранимую форму контейнера.
  Ни то ни другое в эту волну не вошло. Пометка стоит и в самом хелпере, у строки
  установки контейнера. (Прежняя причина «генератор фикстур не собирается» снята:
  переход `tessera_issuer` на `tessera_hashchain` доведён, `cargo xtask codes-fixtures`
  собирается и зелёный — остаётся сама правка хранимой формы.)

- **CODE-008…010, дополнение от 2026-08-17 вечера** — кейсы написаны по находкам
  обязательного ревью, до кода, и красные по одной общей причине: `helpers/codes-phone.sh`
  не имеет нужных подкоманд (`prepare --break-cert-preconditions`, `seal-audit-chain`,
  `expect-no-session`, `rotate-persisted-epoch`, `expect-one-epoch-in-journal`).
  Что они стерегут:
  - CODE-008 — метод кода доступен на устройстве, где предпосылки сертификатного пути
    не выполнены. Ревью нашло, что самопроверка гейтила код-путь загрузкой PKCS#11-модуля
    и ГОСТ-пробой: метод, существующий ровно для машины без работающего сертификатного
    пути, ею же и закрывался;
  - CODE-009 — отклонённый вход не оставляет записи о сессии. Регистрация у демона шла
    до последнего отказа, и у него оставалась сессия, которой не существует;
  - CODE-010 — все события одного входа несут одну эпоху. Ветка PAM брала эпоху из
    конфигурации, ядро — персистированную; пары в сверке строятся по тройке
    «устройство, эпоха, nonce», поэтому расхождение превращало каждый вход в непарный.

- **ENRC (12), `28-codes-enrollment`** — suite заведён 2026-08-17 вместе с change'ем
  `codes-device-artifacts` (часть Codes в пакете первичной настройки). Кейсы написаны
  до прогона: ожидания взяты из спеки `device-enrollment` (и `device-unenroll` для
  ENRC-007), а не измерены. Не прогонялись ни разу, и на сегодня прогон невозможен по
  трём причинам, каждая из которых сама по себе держит suite красным:
  - стенд ставит `.deb` из релизных артефактов main, а части Codes в пакете там ещё
    нет — та же причина, что у `27-codes-phone`;
  - ENRC-007 зовёт `tessera un-enroll`, которой не существует: команду вводит
    change `device-lifecycle`, этот change лишь расширяет её wipe-список. До неё кейс
    упадёт на неизвестной подкоманде;
  - хелпер `codes-enroll.sh` передаёт PIN контейнера доставки ключом
    `--codes-pin-file`. Имя ключа согласовано с реализацией импорта в этой же волне;
    если оно разойдётся, первый прогон покажет это ошибкой разбора аргументов, а не
    отказом продукта.
  Чего эти кейсы намеренно НЕ проверяют: чтения пакета с CCID-токена и партионного
  носителя (раздел 2 change'а) — он едет отдельно и требует живого токена, которого
  не объявляет ни один профиль.

  Кейсы ENRC-008…011 добавлены 2026-08-17 по итогам прохода по модели угроз для того
  же change'а. Прогону сегодня не поддаются по общей причине выше (кода нет в main),
  плюс по своим:
  - **ENRC-008** (хранилище с ослабленными правами не получает ничего) — ослабляет
    `/var/lib/tessera` до `0777` шагом кейса и возвращает права в teardown. Если
    teardown не отработает, следующий suite прогонится на ослабленном каталоге:
    первый же прогон надо смотреть глазами.
  - **ENRC-009** и **ENRC-010** (потолок размера и симлинк в имени из секции)
    подкладывают файл прямо в собранный пакет и потому знают путь `RUN_DIR` хелпера
    `codes-enroll.sh`. Переименование каталога в хелпере сломает эти два кейса
    ошибкой шага, а не отказом продукта. Регексп причины в ENRC-010 намеренно широкий:
    точная формулировка ошибки ядра для `ELOOP` на живой системе не измерена.
  - **ENRC-011** (секция `[codes]` только из подписанных байтов) — **заготовка, к
    прогону не готова**. Ей нужен managed-пакет и правка его манифеста после первого
    импорта, а `codes-enroll.sh` умеет собирать только standalone (подписанный
    манифест потребовал бы ключа организации на стенде) и не имеет ни
    `--managed`, ни `tamper-manifest-codes`, ни `assert-anchor-unchanged`. Первый
    прогон отдаст на этих шагах `ERROR` разбора аргументов. Сама гарантия закрыта
    unit-тестом `a_codes_section_reaches_the_store_only_under_a_signature_that_held`
    (`enrollment/codes_tests.rs`), который красный до правки. Подмена содержимого
    носителя МЕЖДУ проверкой подписи и применением (исходный сценарий находки)
    снаружи не воспроизводится вовсе: профиля с носителем, меняющимся под чтением,
    нет ни одного.

  **ENRC-012 добавлен 2026-08-18** при сужении CODE-007: тот проверял один из двух
  способов снять право оператору, и второй остался без кейса. Кейс проходит обе ветки
  подряд — набор без билета закрывает вход, повторная подача прежнего носителя его
  открывает снова, отзыв списком закрывает необратимо, — потому что порознь ни одна
  ничего не доказывает. Красный по общей причине выше (кода нет в main). Своё:
  - хелперу дописана форма `package --without-ticket`; она вычёркивает билет по номеру
    из манифеста и падает, если вычёркивать нечего, — набор, совпавший с исходным,
    проверил бы обратное обещанному;
  - комплект фикстур несёт РОВНО ОДИН билет, поэтому «набор без билета» сегодня пустой.
    Что импорт принимает пустой набор билетов, снятым поведением не подтверждено: в коде
    отдельного отказа на пустой набор нет (`TicketStore` знает состояние «билетов нет»),
    но первый прогон надо смотреть глазами. Если импорт откажет, кейс красный по стенду,
    а не по гарантии;
  - ожидание последнего шага (отзыв переживает повторную подачу прежнего носителя) взято
    из кода: монотонность списка сверяется, только когда поставка НЕСЁТ список
    (`check_delivered_artefacts`), а поставка без него применённый файл не трогает.
    Спека формулирует это как «приём набора старее применённого отвергается».

- **REC (4), `31-codes-reconcile`** — suite заведён 2026-08-17 под шов между устройством и
  оператором: устройство пишет входы в свою хеш-цепочку, оператор пишет квитанции, и
  `issuer codes reconcile` читает и то, и другое. Кейсами шов закрыт потому, что ломается
  тихо: читатель, разбирающий не тот формат, не падает — он находит ноль строк входа и
  объявляет отчёт ПОЛНЫМ. Ровно это и жило, пока сверка фильтровала по имени
  tracing-события `qr_code_login`, а цепочка писала `op: code_login` (найдено ревью
  2026-08-17, починено в тот же вечер). Не прогонялся ни разу; красным держат две причины:
  - общая с `27-codes-phone`: стенд ставит `.deb` из релизных артефактов main, а этого
    кода в main ещё нет;
  - своя: кейсы требуют, чтобы на устройстве был ВКЛЮЧЁН журнал — либо явным
    `[audit].enabled`, либо связкой с `[codes].enabled` (`validate_audit` берёт
    `raw.enabled.unwrap_or(codes.enabled)`, то есть метод по коду включает журнал сам).
    Это условие стенда, а не разрыв в продукте: установка журнала из конфигурации стоит в
    продакшн-пути аутентификации (`pam_tessera/src/entry.rs`, `install_from_config` до
    начала разговора). Секцию `[codes]` со включённым методом пишет `codes-phone.sh
    prepare`, поэтому в этой сюите условие выполняется само; на стенде, собранном иначе,
    хелпер `codes-reconcile.sh` отдаст 70 («журнал пуст или отсутствует») — сбой стенда,
    а не отказ продукта, и в отчёте это будет видно именно так.
  Что измерено, а что обещано: ожидания на саму сверку сняты локально с того же бинаря —
  `login-without-receipt …` (код 0), `codes-refusal: journal_malformed` (код 1) на изъятую
  из середины строку, `codes-refusal: journal_without_logins` (код 1) на журнал без
  входов, `unsigned-tail from=<seq>` в отчёте, чей хвост не заверен. Обещано, а не
  измерено, ровно одно звено — что вход по коду на живой системе оставляет в журнале
  строку `op: code_login`; его и увидит первый прогон.
  Чего эти кейсы намеренно НЕ проверяют: обрезания хвоста журнала. Хеш-цепочка его не
  ловит — префикс валидной цепочки валиден, — и кейс, обрезающий хвост и ждущий отказа,
  проверял бы обещание, которого механизм не даёт. Границу стережёт REC-004: пока хвост
  не заверен, отчёт обязан говорить об этом рядом со словами «находок нет».

| id | статус | дата | версия | профиль | комментарий |
|---|---|---|---|---|---|
| AUTH-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| AUTH-002 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| AUTH-003 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CHAL-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CLI-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CLI-002 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CLI-003 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CONF-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CONF-002 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CONF-003 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CONF-004 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CONF-005 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CONF-006 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CONF-007 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CONF-008 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CONF-009 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CONF-010 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CONF-011 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CONF-012 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| CONF-013 | FAIL | 2026-08-10 | 0.5.0-1 | astra-vm | Тот же красный, что и на ubuntu-container: спека требует WARN «deprecated and ignored» на `[logging].syslog_facility`, продукт принимает ключ молча. Расхождение спека↔код, одинаковое на обоих профилях |
| ENR-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| ENR-002 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| HOOK-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| HOOK-002 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| HOST-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| HOST-002 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| HOST-003 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| LIC-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| LOG-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| LOG-002 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| LOG-003 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| PMR-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| PMR-002 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| PMR-003 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| PMR-004 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| REV-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| REV-002 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| REV-003 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| REV-004 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| REV-005 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| REV-006 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| ROLE-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| ROLE-002 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| ROLE-004 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| ROLE-005 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| TAG-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| TAG-002 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| TAG-003 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| TAG-004 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| TRUST-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| TRUST-002 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| TRUST-003 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| TRUST-004 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| TRUST-005 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| USB-001 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| USB-002 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| USB-003 | PASS | 2026-08-10 | 0.5.0-1 | astra-vm |  |
| AUTH-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| AUTH-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container | Был красным на дефекте PAM_MAXTRIES (модуль отдавал 8 PAM_CRED_INSUFFICIENT вместо 11); закрыт изменением pam-maxtries-fix, проверено на артефакте сборки 83613888 |
| AUTH-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CHAL-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CLI-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CLI-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CLI-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CONF-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CONF-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CONF-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CONF-004 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CONF-005 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CONF-006 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CONF-007 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CONF-008 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CONF-009 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CONF-010 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CONF-011 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CONF-012 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| CONF-013 | FAIL | 2026-08-10 | 0.5.0-1 | ubuntu-container | Красный намеренно: спека configuration требует WARN «deprecated and ignored» на `[logging].syslog_facility`, продукт принимает ключ молча (ни в выводе `tessera check`, ни при `TESSERA_LOG=debug`). Администратор, задавший ключ, считает журналирование настроенным, а оно не настроено. Расхождение спека↔код не разведено |
| ENR-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| ENR-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| HOOK-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| HOOK-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| HOST-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| HOST-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| HOST-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| INST-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| INST-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| INST-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| INST-004 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| INST-005 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| ISS-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| ISS-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| ISS-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| ISS-004 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| ISS-005 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container | Сквозной кейс, вскрывший четыре дефекта совместимости выпуска и проверки: keyUsage/EKU листа, расходившиеся дефолты profile_version, непригодные рамки делегирования и требование роли. Все закрыты; зелёный с role-account-login |
| ISS-006 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| ISS-007 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| LIC-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| LOG-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| LOG-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| LOG-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| MON-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| MON-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| PAMI-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| PAMI-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| PAMI-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| PMR-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| PMR-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| PMR-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| PMR-004 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| REV-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| REV-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| REV-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| REV-004 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| REV-005 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| REV-006 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| ROLE-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container | Был красным до перепроводки role-selection: модуль спрашивал роль prompt'ом вместо вывода из имени учётной записи входа. Закрыт изменением role-account-login |
| ROLE-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container | Был красным до перепроводки role-selection: модуль спрашивал роль prompt'ом вместо вывода из имени учётной записи входа. Закрыт изменением role-account-login |
| ROLE-004 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container | Был красным до перепроводки role-selection: модуль спрашивал роль prompt'ом вместо вывода из имени учётной записи входа. Закрыт изменением role-account-login |
| ROLE-005 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| SIGN-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| SIGN-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| SIGN-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| SIGN-004 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| TAG-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| TAG-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| TAG-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| TAG-004 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| TOK-001 | SKIP | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| TOK-002 | SKIP | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| TOK-003 | SKIP | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| TOK-004 | SKIP | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| TRUST-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| TRUST-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| TRUST-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| TRUST-004 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| TRUST-005 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| USB-001 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| USB-002 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
| USB-003 | PASS | 2026-08-10 | 0.5.0-1 | ubuntu-container |  |
