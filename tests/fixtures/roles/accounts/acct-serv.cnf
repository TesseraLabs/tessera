# OpenSSL config — role-account leaf: login account `serv`, role `serv` covered.
#
# Target role model ("role = role account"): the engineer logs into the role
# account by name (`ssh serv@device`), so the requested role equals the login
# account name and pam_cert_allowed_roles must list it.
#
# Used by gen-role-account-certs.sh; signed by the CA in ../ca.{key,crt}.pem —
# the same trust anchor as the suffix-model leaves one level up.
#
# OIDs (see crates/tessera_core/src/x509/oids.rs — do NOT change):
#   2.25.183976554325829274683049824615098          HOST_BINDING_OID
#   2.25.185305973969816596290730578528098241367    ALLOWED_ROLES_OID
#
# DER payload for SEQUENCE { UTF8String "serv" }:
#   30 06 0c 04 73 65 72 76
#   ^^ ^^                      SEQUENCE, content length = 6 octets
#      ^^ 0c 04 73 65 72 76    UTF8String "serv"            (6 octets)
[ req ]
default_bits        = 2048
default_md          = sha256
prompt              = no
distinguished_name  = dn
req_extensions      = leaf_ext

[ dn ]
CN = acct-serv
O  = tessera-test
C  = RU

[ leaf_ext ]
basicConstraints       = critical, CA:FALSE
keyUsage               = critical, digitalSignature, keyEncipherment
extendedKeyUsage       = clientAuth, emailProtection
subjectKeyIdentifier   = hash
# pam_cert_host_binding = ["*"]  (wildcard — authorises any host, test leaf)
2.25.183976554325829274683049824615098 = DER:30:03:0c:01:2a
# pam_cert_allowed_roles = [serv]  (non-critical) — role `serv` is covered
2.25.185305973969816596290730578528098241367 = DER:30:06:0c:04:73:65:72:76
