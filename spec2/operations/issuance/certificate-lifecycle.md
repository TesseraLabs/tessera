---
id: certificate-lifecycle
kind: операция
context: issuance
name: "Жизненный цикл выпуска сертификатов"
aliases:
  - "cert-issuance"
relations:
  references:
    - issuance.parent-ca-certificate
    - issuance.leaf-issuance-scope
anchors:
  code:
    - "crates/tessera_issuer/src/lib.rs"
  test:
    - "tests/e2e/cases/60-issuer.yaml"
requirements:
  CISS-001:
    kind: операционное
    subjects:
      - issuance.certificate-lifecycle
    origins:
      - "cert-issuance::Сборка TBS с расширениями Tessera"
    evidence:
      code:
        - "crates/tessera_issuer/src/lib.rs"
  CISS-002:
    kind: инвариант
    subjects:
      - issuance.certificate-lifecycle
    origins:
      - "cert-issuance::Монотонное сужение рамок до подписи"
    evidence:
      test:
        - "tests/e2e/cases/60-issuer.yaml"
  CISS-003:
    kind: операционное
    subjects:
      - issuance.certificate-lifecycle
    origins:
      - "cert-issuance::Выпуск листа по CSR"
    evidence:
      code:
        - "crates/tessera_issuer/src/lib.rs"
  CISS-004:
    kind: инвариант
    subjects:
      - issuance.certificate-lifecycle
    origins:
      - "cert-issuance::Самоподписанный корень парка"
    evidence:
      test:
        - "tests/e2e/cases/60-issuer.yaml"
  CISS-005:
    kind: операционное
    subjects:
      - issuance.certificate-lifecycle
    origins:
      - "cert-issuance::Случайные 128-битные серийные номера"
    evidence:
      code:
        - "crates/tessera_issuer/src/lib.rs"
  CISS-006:
    kind: операционное
    subjects:
      - issuance.certificate-lifecycle
    origins:
      - "cert-issuance::Выпуск CRL с монотонным crlNumber"
    evidence:
      code:
        - "crates/tessera_issuer/src/lib.rs"
  CISS-007:
    kind: контракт
    subjects:
      - issuance.certificate-lifecycle
    origins:
      - "cert-issuance::Контракт с проверкой Engine"
    evidence:
      test:
        - "tests/e2e/cases/60-issuer.yaml"
  CISS-008:
    kind: операционное
    subjects:
      - issuance.certificate-lifecycle
    origins:
      - "cert-issuance::Пригодность выпускаемых рамок делегирования"
    evidence:
      code:
        - "crates/tessera_issuer/src/lib.rs"
---
# Жизненный цикл выпуска сертификатов

Эта страница заменяет процедурную capability-спеку OpenSpec «cert-issuance»: она
помещает каждое прежнее требование в адресуемую норму с владельцем, видом и
проверяемым происхождением. Под каждым нормативным предложением сохранены подробные условия, отрицательные
сценарии и rationale; они входят в ту же страницу и уточняют границы нормы.

### CISS-001 — Сборка TBS с расширениями Tessera

[[issuance.certificate-lifecycle|Жизненный цикл выпуска сертификатов]] MUST соблюдать правило «Сборка TBS с расширениями Tessera» вместе со всеми условиями и отказами, зафиксированными ниже.

Мигрировано из openspec/specs/cert-issuance/spec.md, requirement «Сборка TBS с расширениями Tessera».

Ядро должно собирать TBS-структуры двух профилей — лист смены инженера и
CA организации — с custom-расширениями Tessera (host-binding/user-binding, теги,
рамки делегирования) под теми же OID-дугами `2.25.<UUID>` и в том же
DER-кодировании, которые проверяет Engine. Определения OID и DER-схем должны
(обязательно) переиспользоваться из разделяемого с Engine модуля, а не дублироваться.
TBS с неполным набором обязательных для профиля расширений не должен
передаваться на подпись.

Профиль листа смены должен, помимо расширений Tessera, нести стандартные
расширения, без которых сертификат не пригоден для аутентификации: `keyUsage`
с битом `digitalSignature` и `extendedKeyUsage` с `clientAuth`. Их отсутствие
означает, что выпущенное удостоверение будет отвергнуто проверкой Engine
(`pre_validate_end_entity`) — то есть выпуск формально успешен, а результат
непригоден.

Профиль CA организации должен нести `basicConstraints` (cA=TRUE) и
`keyUsage` с `keyCertSign`, без которых сертификат не пригоден как звено цепочки
доверия.

Набор расширений каждого профиля должен проверяться против той же функции
проверки, которой пользуется Engine, а не против собственных ожиданий выпуска:
согласованность двух половин продукта не следует из корректности каждой в
отдельности.

#### Scenario: Выпуск листа смены с полным набором расширений
- **WHEN** оператор запрашивает выпуск листа смены с host-binding, тегами и рамками
- **THEN** ядро собирает TBS, в котором каждое расширение закодировано байт-в-байт так, как его ожидает разбор Engine

#### Scenario: Обязательное расширение профиля не заполнено
- **WHEN** запрос на выпуск листа не содержит обязательного для профиля расширения
- **THEN** сборка TBS отвергается с объяснением до какой-либо подписи (fail-closed)

#### Scenario: Выпущенный лист проходит проверку Engine
- **WHEN** лист выпущен штатной операцией выпуска
- **THEN** он проходит `pre_validate_end_entity` без ошибок `KeyUsage` и `Eku` — выпуск и проверка согласованы, а не только корректны по отдельности

### CISS-002 — Монотонное сужение рамок до подписи

[[issuance.certificate-lifecycle|Жизненный цикл выпуска сертификатов]] MUST соблюдать правило «Монотонное сужение рамок до подписи» вместе со всеми условиями и отказами, зафиксированными ниже.

Мигрировано из openspec/specs/cert-issuance/spec.md, requirement «Монотонное сужение рамок до подписи».

Ядро должно до передачи TBS на подпись проверить монотонное сужение рамок
делегирования: рамки выпускаемого сертификата ⊆ рамки родительского CA ⊆ рамки
корня (предикат — тот же разделяемый код, что использует Engine). Нарушение
монотонности должно прерывать выпуск до подписи; частично подписанных или
«условно выпущенных» артефактов не должно возникать.

#### Scenario: Рамки листа шире рамок родительского CA
- **WHEN** запрошенный лист содержит рамки, не покрываемые рамками CA организации
- **THEN** выпуск отвергается до подписи, с указанием конкретного нарушенного измерения рамок

#### Scenario: Рамки равны родительским
- **WHEN** запрошенные рамки в точности совпадают с рамками родителя
- **THEN** проверка проходит (сужение нестрогое), выпуск продолжается

### CISS-003 — Выпуск листа по CSR

[[issuance.certificate-lifecycle|Жизненный цикл выпуска сертификатов]] MUST соблюдать правило «Выпуск листа по CSR» вместе со всеми условиями и отказами, зафиксированными ниже.

Мигрировано из openspec/specs/cert-issuance/spec.md, requirement «Выпуск листа по CSR».

Ядро должно поддерживать выпуск листа смены по CSR (PKCS#10) как
равноправный режим наряду с прямой передачей публичного ключа оператором:
ключ и subject берутся из CSR, самоподпись CSR (доказательство владения
ключом) должна проверяться до выпуска, невалидная — прерывать выпуск
до подписи. Запрошенные в CSR атрибуты и расширения не должны
влиять на скоуп выпускаемого сертификата: рамки, роли, binding'и и уровень
задаёт исключительно оператор; значения из CSR допустимы только как
предзаполнение форм с явной пометкой источника.

#### Scenario: Валидный CSR
- **WHEN** оператор подаёт CSR с корректной самоподписью и задаёт скоуп выпуска
- **THEN** лист выпускается с ключом и subject из CSR и скоупом, заданным оператором

#### Scenario: Битая самоподпись CSR
- **WHEN** самоподпись CSR не проходит проверку ключом из этого же CSR
- **THEN** выпуск отвергается до какой-либо подписи, с объяснением

#### Scenario: CSR запрашивает расширения
- **WHEN** CSR содержит атрибуты с запрошенными расширениями (роли, binding'и)
- **THEN** в выпущенный сертификат попадают только значения, заданные оператором; запрошенное в CSR на состав расширений не влияет

### CISS-004 — Самоподписанный корень парка

[[issuance.certificate-lifecycle|Жизненный цикл выпуска сертификатов]] MUST соблюдать правило «Самоподписанный корень парка» вместе со всеми условиями и отказами, зафиксированными ниже.

Мигрировано из openspec/specs/cert-issuance/spec.md, requirement «Самоподписанный корень парка».

Ядро должно поддерживать выпуск самоподписанного корневого сертификата
парка: CA-профиль, где издатель равен субъекту, с назначением корневых рамок
делегирования и подписью тем же интерфейсом подписи (ключ корня — в
токене/HSM/Vault, как у любой операции). Без этой операции serverless-модель
не бутстрапится. Самопроверка после подписи обязательна, как для всех профилей.

#### Scenario: Бутстрап корня
- **WHEN** владелец парка выпускает корень с корневыми рамками на ключе из PKCS#11
- **THEN** получается самоподписанный CA-сертификат, под которым проходят выпуск CA организаций и вся цепочка проверок Engine

### CISS-005 — Случайные 128-битные серийные номера

[[issuance.certificate-lifecycle|Жизненный цикл выпуска сертификатов]] MUST соблюдать правило «Случайные 128-битные серийные номера» вместе со всеми условиями и отказами, зафиксированными ниже.

Мигрировано из openspec/specs/cert-issuance/spec.md, requirement «Случайные 128-битные серийные номера».

Ядро должно присваивать серийные номера как случайные 128-битные
положительные числа из CSPRNG (RFC 5280: положительное целое, не длиннее
20 октетов). Ядро не должно требовать координации или общего
состояния между независимыми операторами для уникальности серийников.

#### Scenario: Два оператора выпускают независимо
- **WHEN** два экземпляра инструмента без общего состояния выпускают сертификаты от одного CA
- **THEN** серийники статистически уникальны, положительны и укладываются в 20 октетов, координация не требуется

### CISS-006 — Выпуск CRL с монотонным crlNumber

[[issuance.certificate-lifecycle|Жизненный цикл выпуска сертификатов]] MUST соблюдать правило «Выпуск CRL с монотонным crlNumber» вместе со всеми условиями и отказами, зафиксированными ниже.

Мигрировано из openspec/specs/cert-issuance/spec.md, requirement «Выпуск CRL с монотонным crlNumber».

Ядро должно выпускать CRL от имени CA: список отозванных серийников из
локального стейта и `crlNumber` строго больше последнего выпущенного этим стейтом
(значение фиксируется в журнале выпусков). Попытка выпустить CRL с `crlNumber`,
не превышающим последний известный, должна отвергаться. Формат CRL должен
(обязательно) соответствовать тому, что принимает проверка `revocation`.

#### Scenario: Очередной CRL
- **WHEN** оператор выпускает новый CRL после отзыва сертификата
- **THEN** `crlNumber` строго больше предыдущего, состав включает все отозванные серийники из стейта

#### Scenario: Откат crlNumber
- **WHEN** стейт повреждён и предлагает `crlNumber`, не превышающий зафиксированный в журнале
- **THEN** выпуск CRL отвергается до подписи, с указанием ожидаемого минимального номера

### CISS-007 — Контракт с проверкой Engine

[[issuance.certificate-lifecycle|Жизненный цикл выпуска сертификатов]] MUST соблюдать правило «Контракт с проверкой Engine» вместе со всеми условиями и отказами, зафиксированными ниже.

Мигрировано из openspec/specs/cert-issuance/spec.md, requirement «Контракт с проверкой Engine».

Каждый собранный сертификатный артефакт должен до выдачи оператору
проходить самопроверку тем же разделяемым кодом разбора/валидации расширений,
которым пользуется Engine. Артефакт, который эта самопроверка не принимает,
не должен выдаваться как результат выпуска.

#### Scenario: Самопроверка после подписи
- **WHEN** подпись получена и сертификат собран
- **THEN** артефакт прогоняется через разделяемый код валидации; при отказе результат не выдаётся, операция помечается ошибкой

### CISS-008 — Пригодность выпускаемых рамок делегирования

[[issuance.certificate-lifecycle|Жизненный цикл выпуска сертификатов]] MUST соблюдать правило «Пригодность выпускаемых рамок делегирования» вместе со всеми условиями и отказами, зафиксированными ниже.

Мигрировано из openspec/specs/cert-issuance/spec.md, requirement «Пригодность выпускаемых рамок делегирования».

Выдача не должна порождать сертификат, который по собственным
правилам проверки Engine не может быть использован ни в одном сценарии входа.
Рамки делегирования должны проверяться на пригодность до подписи, а не
только на монотонность относительно родителя.

Непригодными должны считаться рамки, чьё измерение не допускает ни одного
значения: пустой `allowRoles` (закрытый белый список без элементов не разрешает
ни одной роли) и нулевой `maxTtl` (дочернее звено обязано иметь нулевой срок
действия). Обнаружение непригодности должно прерывать выдачу с указанием
конкретного измерения; частично подписанных артефактов и записей в журнале
выдачи не должно возникать.

Значения по умолчанию для измерений рамок должны быть пригодными в
указанном смысле. Умолчание не должно вводиться там, где пригодное
значение зависит от внедрения (имена ролей): в таком случае аргумент должен
(обязательно) быть обязательным, поскольку угаданное умолчание либо повторяет
непригодность, либо расширяет рамки сверх названного оператором.

Умолчание `maxTtl` должно соответствовать тому, что рамка ограничивает:
у корня парка это срок CA организации, у CA организации — срок листа смены.
Единое значение для обеих операций непригодно по существу.

#### Scenario: Выдача CA без разрешённых ролей
- **WHEN** оператор запрашивает выпуск CA, не назвав ни одной разрешённой роли
- **THEN** выдача отвергается до подписи с указанием, что пустой `allowRoles` не разрешает ни одной роли

#### Scenario: Явно нулевой потолок срока
- **WHEN** оператор задаёт `maxTtl = 0`
- **THEN** выдача отвергается до подписи: под таким CA не проходит ни одно дочернее звено с ненулевым сроком

#### Scenario: Умолчания дают пригодный сертификат
- **WHEN** оператор задаёт только обязательные аргументы выпуска CA
- **THEN** выпущенный сертификат пригоден: под ним проходит выдача листа смены и проверка цепочки Engine
