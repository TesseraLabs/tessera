<?xml version="1.0" encoding="UTF-16"?>
<!--
  Windows Task Scheduler definition for the Tessera issuer local signing agent.

  Install (per user, no admin), from a PowerShell prompt:
    schtasks /Create /TN "Tessera Issuer Serve" /XML issuer-serve-task.xml

  Or import it via Task Scheduler > Import Task. Edit the <Command> and
  <Arguments> for your install path, token module, key label and cabinet origin.

  The pairing token is written to %LOCALAPPDATA%\tessera-issuer\token, protected
  by the user profile's ACLs. The PIN is entered via pinentry (Gpg4win) or the
  console. The task runs at logon in the interactive user session (the agent
  listens only on 127.0.0.1 and needs no elevation).
-->
<Task version="1.2" xmlns="http://schemas.microsoft.com/windows/2004/02/mit/task">
  <RegistrationInfo>
    <Description>Tessera issuer local signing agent (127.0.0.1 only)</Description>
  </RegistrationInfo>
  <Triggers>
    <LogonTrigger>
      <Enabled>true</Enabled>
    </LogonTrigger>
  </Triggers>
  <Principals>
    <Principal id="Author">
      <LogonType>InteractiveToken</LogonType>
      <RunLevel>LeastPrivilege</RunLevel>
    </Principal>
  </Principals>
  <Settings>
    <MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>
    <DisallowStartIfOnBatteries>false</DisallowStartIfOnBatteries>
    <StopIfGoingOnBatteries>false</StopIfGoingOnBatteries>
    <StartWhenAvailable>true</StartWhenAvailable>
    <RestartOnFailure>
      <Interval>PT1M</Interval>
      <Count>3</Count>
    </RestartOnFailure>
  </Settings>
  <Actions Context="Author">
    <Exec>
      <Command>C:\Program Files\Tessera\issuer.exe</Command>
      <Arguments>serve --daemon-token-file --allow-origin https://cabinet.example --module "C:\Program Files\OpenSC Project\OpenSC\pkcs11\opensc-pkcs11.dll" --key tessera-ca --algorithm ecdsa-p256</Arguments>
    </Exec>
  </Actions>
</Task>
