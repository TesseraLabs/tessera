{
  "name": "tessera-issuer-cabinet",
  "version": "0.1.0",
  "private": true,
  "description": "Serverless web cabinet for Tessera certificate issuance (SPA + WASM core).",
  "type": "module",
  "license": "SEE LICENSE IN ../LICENSE",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test src/**/*.test.ts",
    "build": "./build.sh"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "esbuild": "0.28.1",
    "typescript": "5.7.3"
  }
}
