## 0. Диагноз (до всякого кода)

- [x] 0.1 **ПОДТВЕРЖДЕНО на стенде 2026-08-10/11.** Отказ `ENGINE_ctrl_cmd_string sequence (SO_PATH=1, ID=1, LOAD=0)` воспроизведён на Astra 1.8.4 (стенд профиля `astra-vm`) с реальным ГОСТ-2012-256 удостоверением. `clear_ambient_registration` находит ambient-запись и снимает её успешно (обе операции возвращают успех — видно по инструментированной сборке), следующий `LOAD` всё равно даёт `0`
- [x] 0.2 Fallback «подобрать уже зарегистрированный движок» реализован и проверен на стенде — **не работает**: `by_id` отдаёт «no engine registered under id gost». Конфликта на уровне таблицы ENGINE в момент отказа нет, дело внутри движка. Код удалён полностью
- [x] 0.3 Первопричина найдена чтением исходников `gost-engine` (`gost_eng.c`, `check_gost_engine`): process-global guard `ameth_GostR3410_2001` выставляется при первой успешной инициализации и не сбрасывается `gost_engine_destroy` — тот чистит таблицу методов, но не флаг. Повторная инициализация в том же процессе невозможна из OpenSSL API вообще
- [x] 0.4 Модель процессов объясняет плавающую воспроизводимость: `sudo`/`login`/`sshd` живут одну попытку входа (гонки нет), `fly-dm` — один процесс на весь uptime дисплея (движок либо загрузился первым входом, либо сломан до перезапуска службы)
- [x] 0.5 NID сверены с `obj_mac.h`: Streebog-256/512 = `982`/`983` (`md_gost12_256`/`md_gost12_512`); захардкоженные в модуле `1177`/`1178` = `kuznyechik_ctr_acpkm` / `kuznyechik_ctr_acpkm_omac`, шифр «Кузнечик»

## 1. Загрузка движка

- [x] 1.1 Гейт `self_check` расширен с `needs_gost()` на `gost_engine_path.is_some()` — загрузка tessera становится первым обращением к libcrypto в процессе
- [x] 1.2 `ensure_loaded_if_any_gost` (путь верификации цепочки) не тронут — остаётся ленивым
- [x] 1.3 Устройство без `gost_engine_path` движок не трогает: RSA-only сценарий сохранён
- [x] 1.4 Проверено на стенде: `no pre-existing registration found` → `load_rc=1` → полный цикл `pamtester fly-dm serv authenticate acct_mgmt open_session close_session`, дважды подряд
- [x] 1.5 Проверено сильнее `pamtester`: реальный интерактивный вход в `fly-dm` с USB-носителя (privilege separation, не один процесс на весь диалог)
- [x] 1.6 Изолирующая проверка: с пустым allow-list движок грузится так же (`load_rc=1`), а отказ приходит позже, на проверке allow-list, — значит загрузка и allow-list развязаны

## 2. Streebog по имени

- [x] 2.1 `algorithms.rs` резолвит дайджесты через `MessageDigest::from_name`, хардкод NID удалён
- [x] 2.2 Doc-комментарий модуля приведён в соответствие с кодом (раньше описывал резолюцию по имени, которой не было)

## 3. Диагностика в `tessera check`

- [x] 3.1 Новый модуль `crates/tessera_cli/src/startup_check/gost.rs`, включён в pipeline
- [x] 3.2 Четыре записи: `gost_engine_ok` (INFO), `gost_pkcs11_unsupported` (WARN), `gost_engine_configured_unused` (WARN), `gost_engine_load_failed` (ERROR)
- [x] 3.3 Хост без ГОСТ не получает ни одной записи
- [x] 3.4 Проверка выполняет ту же `ensure_loaded`, что и аутентификация
- [x] 3.5 Юнит-тесты на все четыре ветки
- [x] 3.6 Сообщение валидатора для `crypto_backend = "pkcs11-native"` с ГОСТ в allow-list объясняет, что путь к движку ситуацию не исправит (ветка недостижима сегодня — `needs_gost()` сама требует OpenSSL-бэкенд, — но формулировка не должна вводить в заблуждение, если инвариант изменится)
- [x] 3.7 Проверено на живом конфиге: WARN при пустом allow-list (exit 0), INFO при корректной конфигурации (`8 info, 1 warn, 0 error`), ERROR при пути на существующий, но нерабочий файл (`7 info, 1 warn, 1 error`, exit 1)
- [x] 3.8 Измерено, что несуществующий путь до этого шага **не доходит**: его отклоняет `validate_gost_engine_path` при загрузке конфига (`ERROR: failed to load config ...`), pipeline не запускается. Два независимых барьера

## 4. Спеки

- [x] 4.1 `gost-crypto` → «Ленивая загрузка engine»: загрузка в `self_check` привязана к конфигурации, верификация цепочки остаётся ленивой; добавлено обоснование через guard движка
- [x] 4.2 `gost-crypto` → «Алгоритмы»: неверные NID `1177`/`1178` убраны, зафиксирована резолюция по имени
- [x] 4.3 `cli-diagnostics` → «tessera check»: ГОСТ-шаг добавлен в перечень pipeline, четыре записи описаны таблицей, добавлены сценарии

## 5. E2E-кейсы

- [ ] 5.1 Профилю нужна возможность, означающая «на машине есть работающий gost-engine»: в `astra-vm` она не объявлена, а занижать `requires` ради зелёного прогона в контейнере запрещено. Ввести и объявить только там, где движок реально стоит
- [ ] 5.2 Кейс: корректная ГОСТ-конфигурация → `gost_engine_ok`, exit 0
- [ ] 5.3 Кейс: путь задан, ГОСТ-подписи не разрешены → `gost_engine_configured_unused`, exit 0
- [ ] 5.4 Кейс: путь на существующий, но нерабочий файл → `gost_engine_load_failed`, exit 1
- [ ] 5.5 Кейс: ГОСТ разрешён без `gost_engine_path` → конфигурация отклоняется на загрузке, pipeline не запускается (сценарий спеки `gost-crypto`, отличать от 5.4)
- [ ] 5.6 Ожидания взяты из измерений на стенде, не сочинены; teardown возвращает `/etc/tessera/config.toml` в исходное состояние при любом исходе
- [ ] 5.7 `cargo test -p xtask` — реестр грузится
- [ ] 5.8 Прогон и осознанная запись результата в `tests/e2e/BASELINE.md`

## 5a. Найдено попутно, не чинится здесь

- [ ] 5a.1 `engine.rs` при sanity-проверке после загрузки принимает `md_gost12_256` ИЛИ `streebog256` (форки именуют по-разному), а `algorithms.rs` резолвит только `md_gost12_256`/`md_gost12_512`. На форке со вторым именованием загрузка объявляется успешной, а первый запрос дайджеста отдаёт `DigestUnavailable`. Расхождение старше этого изменения; чинить без воспроизводящего кейса нельзя, а форка с таким именованием под рукой нет — ожидание пришлось бы сочинить

## 6. Документация

- [x] 6.1 `troubleshooting.md` §10 (RU+EN): симптом `LOAD=0`, первопричина, что уже исправлено, рекомендация про `default_algorithms = ALL`; таблица четырёх записей `tessera check`
- [x] 6.2 `configuration.md` (RU+EN): подраздел о жизненном цикле `gost-engine` в процессе рядом с существующим про PKCS#11
- [x] 6.3 `operations.md` §5 (RU+EN): `sudo tessera check` как основная проверка после обновления Astra, впереди разового `openssl engine gost -t`
- [x] 6.4 `changelog.md` (RU): записи про NID, гонку загрузки и новую диагностику
