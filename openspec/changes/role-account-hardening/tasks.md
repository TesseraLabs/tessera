# Tasks: role-account-hardening

- [ ] 1. Решить: зарезервированные имена запрещены в `RoleId` или это ответственность Census. Зафиксировать решение в спеке
- [ ] 2. Реализовать выбранное; список зарезервированных имён вынести туда, где его видно (не хардкод посреди валидатора)
- [ ] 3. Тесты: `root`/`daemon`/`nobody` отвергаются как `role_id` (или, при втором варианте, тест на то, что срез с таким именем не грузится)
- [ ] 4. `docs/{ru,en}/install.md`: закрыть остальные пути входа в ролевую УЗ явными командами (`passwd -l`, отсутствие `authorized_keys`, `AuthenticationMethods`, ограничение `su`)
- [ ] 5. `docs/{ru,en}/configuration.md`: гарантия «вход закрыт всеми путями» либо подпирается, либо снимается
- [ ] 6. Тест совместимости: лист с отозванным `pam_cert_user_binding` проходит проверку; расширение игнорируется как неизвестное non-critical
- [ ] 7. После (6) решить судьбу фикстур суффиксной модели (`tests/fixtures/roles/role-*.cnf`, `gen-role-certs.sh`, `vagrant/scripts/test-roles.sh`)
