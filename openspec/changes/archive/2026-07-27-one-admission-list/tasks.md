# Tasks: one-admission-list

- [x] 1. `tessera_issuer`: убрать `--user` у `issue-leaf`, поле `user_binding` из `LeafRequest`, выпуск расширения из `leaf_extensions`
- [x] 2. `tessera_core`: убрать `verify_user_binding` (`host_binding.rs`), модуль `x509/user_binding_ext.rs` и его реэкспорты — если ничего не читает расширение
- [x] 3. `tessera_core`: удалить `mapping.rs` (legacy-сопоставление по CN/SAN) и `UserMapping` из конфига
- [x] 4. Конфиг: секция `[[user_mapping]]` отвергается адресной диагностикой (как удалённый `[roles].enforce`), а не общим «unknown field»
- [x] 5. `pam_tessera/src/flow.rs`: `authorize_user` решает допуск по `allowed_roles`; `Deps.user_mappings` уходит; трёхисходной развилки больше нет
- [x] 6. Проверить `hooks/vars.rs` — упоминание `user_binding` в контракте переменных хуков; если переменная реально отдаётся, решение о её судьбе описать в отчёте, НЕ выдумывать
- [x] 7. Тесты: допуск по `allowed_roles`; отказ при роли вне списка; отказ при отсутствии расширения; конфиг с `[[user_mapping]]` отвергается с ожидаемым текстом. Тесты legacy-сопоставления удалить, а не переписать
- [x] 8. Фикстуры: `tests/fixtures/roles/accounts/*.cnf` и `crates/tessera_core/tests/fixtures/gen.sh` — убрать выпуск `user_binding`; перевыпустить. Серийники и открытые ключи фикстур ядра НЕ менять (на них завязаны OCSP/CRL)
- [x] 9. e2e: снять кейс `ROLE-003` — его гарантия схлопнулась в `ROLE-002`; фикстура `acct-foreign` теряет смысл
- [x] 10. `dist/config/config.toml.example`: убрать `[[user_mapping]]`
- [x] 11. `docs/{ru,en}/`: `install.md` (три расширения → два), `configuration.md` (раздел про `[[user_mapping]]`), `cert-issuance.md`, `architecture.md` — везде, где описан допуск по учётной записи
- [x] 12. `docs/ru/changelog.md` — Breaking, с миграцией
- [x] 13. Прогон e2e — все 22 кейса зелёные, ROLE-003 снят. Потребовалась правка набора выпуска: кейсы передавали удалённый `--user`, а ISS-003 проверял наличие отозванного расширения. Теперь ISS-003 проверяет две оси рамок и ОТСУТСТВИЕ отозванного OID — с отдельной проверкой разбора сертификата, иначе сбой openssl выглядел бы как «расширения нет»

## Найдено ревью (три независимых прохода)

- [x] 14. Фаза сессии действовала от непроверенного `PAM_USER` с fail-open на CN удостоверения — сверка с ролью из контекста, отказ закрыто
- [x] 15. Инвариант «роль = `PAM_USER`» был лишь верен, но не выражен: `requested` убран из `RoleStage`, расхождение стало непредставимым
- [x] 16. Версия релиза равнялась уже выпущенной (0.4.0-1) — парк не получил бы ломающее изменение через `apt upgrade`; поднято до 0.5.0
- [x] 17. Staged-дерево debhelper попало в git (собранный модуль со старой политикой + пример конфига с удалённой секцией) — выведено из-под контроля, занесено в игнор
- [x] 18. `docs/{ru,en}/issuer.md` учили вызывать удалённый `--user`
- [x] 19. Дельта `role-selection` — требование допуска и новое требование о фазе сессии
- [x] 20. ПЕРЕНЕСЕНО в change `role-account-hardening`: Зарезервированные имена ролей: пространство имён ролей совпало с системными УЗ, `root`/`daemon`/`nobody` проходят синтаксис. Решить — запрет в `RoleId` или ответственность Census, зафиксировать в спеке. Отдельным изменением
- [x] 21. ПЕРЕНЕСЕНО в change `role-account-hardening`: `docs/{ru,en}/install.md` заводит ролевую УЗ с логин-шеллом, не закрывая `ssh`/`su`/`sudo -u`, при том что документация обещает «вход закрыт всеми путями кроме Tessera». Цена обхода выросла с личной УЗ до общей ролевой
- [x] 22. ПЕРЕНЕСЕНО в change `role-account-hardening`: Заявление о совместимости (старый лист с отозванным расширением проходит) не покрыто тестом; фикстуры суффиксной модели — единственная его опора
- [x] 23. Синхронизация `openspec/specs/README.md` при архивации

