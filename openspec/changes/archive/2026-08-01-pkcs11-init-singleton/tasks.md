## 1. Красные тесты

- [x] 1.1 Заменить `os_mode_allows_concurrent_calls` (`pkcs11_locking.rs:120–145`) на live-тест, который из N нитей одновременно вызывает `Pkcs11Backend::load` для одного модуля и делает по одному реальному cryptoki-вызову; гейт по `PKCS11_MODULE_PATH`, как в остальных live-тестах
- [x] 1.2 Тест: повторный `load` того же пути при живом первом бэкенде не приводит ко второму `C_Initialize` (счётчик инициализаций или наблюдаемый эффект — без обращения к приватным полям)
- [x] 1.3 Тест: поведение `load` после `drop` последнего бэкенда зафиксировано тестом (инвариант перевёрнут в 6.2 — контекст переиспользуется, финализации нет)
- [x] 1.4 Тест: два разных пути модуля в одном процессе получают независимые контексты
- [x] 1.5 Тест: дефолтный `pkcs11_locking_mode` при отсутствии ключа в конфиге равен `mutex` (`config_pkcs11.rs`)
- [x] 1.6 Убедиться, что все тесты группы 1 падают на текущем коде по ожидаемой причине, а не по опечатке

## 2. Процесс-глобальный контекст

- [x] 2.1 Ввести процесс-глобальный реестр контекстов в `backend.rs`; ключ — канонизированный путь модуля (по итогам ревью — сильные ссылки, см. 6.1)
- [x] 2.2 Переписать `Pkcs11Backend::load`: под локом реестра взять живой `Arc<Pkcs11>` либо создать новый и выполнить `C_Initialize` — сам вызов внутри критической секции, независимо от `LockingMode`
- [x] 2.3 Перевести владение контекстом в `Pkcs11Backend` на `Arc<Pkcs11>`; поправить `ctx()` и сигнатуры сиблингов (`session.rs`, `cert_lookup.rs`, `key_lookup.rs`, `sign.rs`, `info.rs`, `waiter.rs`)
- [x] 2.4 Не допускать роста карты при многократных load/drop — одна запись на путь модуля (после 6.1 записи живут до конца процесса намеренно)
- [x] 2.5 Прогнать тесты 1.2–1.4 — зелёные

## 3. Смена дефолта локинга

- [x] 3.1 `RawPkcs11LockingMode`: перенести `#[default]` с `Os` на `Mutex` (`config/raw.rs:306–314`)
- [x] 3.2 Убрать `warn!`-строку про «Mutex mode требуется для legacy-провайдеров» из `backend.rs:86–93` или переформулировать: дефолт больше не является исключительным случаем
- [x] 3.3 Переписать док-комментарий `locking.rs:1–34` — утверждение про «modern Rutoken / ESMART providers» неверно, заменить на факт о дефекте `rtpkcs11ecp` 2.14.1 без обещаний за весь рынок
- [x] 3.4 Прогнать тест 1.5 — зелёный

## 4. Проверка на живом железе

- [x] 4.1 Прогнать live-тесты на Рутокене многопоточно (без `--test-threads=1`) — процесс не падает
- [x] 4.2 Повторить репродьюсер конкурентного `C_Initialize` через публичный API `Pkcs11Backend` — падения нет
- [x] 4.3 Прогнать полный набор на SoftHSM2 — регрессии нет
- [x] 4.4 Проверить, что явный `pkcs11_locking_mode = os` по-прежнему разбирается и применяется

## 5. Документация и сопровождение

- [x] 5.1 Обновить описание `pkcs11_locking_mode` в `docs/ru` и `docs/en`: дефолт `mutex`, основание, цена
- [x] 5.2 Запись в changelog со смены поведения по умолчанию
- [x] 5.3 Зафиксировать в runbook живой проверки токенов, что многопоточный прогон — обязательный сценарий приёмки
- [x] 5.4 `master-code-reviewer` по всему диффу; CRITICAL/HIGH закрыть до мержа

## 6. Отказ от финализации (по итогам ревью)

- [x] 6.1 Убрать `C_Finalize` и `Drop for Pkcs11Backend`; реестр перевести на сильные ссылки — снимает C-1 (finalize при живой сессии), H-1 (пропуск при конкурентном drop), H-2 (потеря записи при ошибке), H-3 (деинициализация у соседей по процессу)
- [x] 6.2 Тест: после уничтожения всех бэкендов повторный `load` не делает второй `C_Initialize`
- [x] 6.3 Тест на остаточный логин: две последовательные аутентификации в одном процессе, вторая обязана начинаться с незалогиненного токена (сценарий `fly-dm`, где процесс живёт весь uptime)
- [x] 6.4 M-3: зафиксировать режим локинга за контекстом (первый выигравший) и логировать WARN при расхождении — сейчас бэкенд в режиме `os` не берёт лок, разделяя контекст с бэкендом в режиме `mutex`
- [x] 6.5 M-4: вернуть дешёвый юнит-тест «в режиме `os` мьютекс не берётся» — после замены live-теста этот инвариант на CI не проверяется вообще
- [x] 6.6 M-5: тест на конкурентный `drop`; убрать тривиально истинный `assert_ne!` на путях модуля
- [x] 6.7 L-1: проверять отсутствие конкретного ключа в реестре вместо процесс-глобальной пустоты
- [x] 6.8 L-2: логировать WARN при провале канонизации пути (два написания одного файла дают два контекста)
- [x] 6.9 L-3: в документации указать, что контекст живёт до конца процесса и `C_Finalize` не вызывается
- [x] 6.10 Повторное ревью диффа после правок

## 7. Второй круг ревью

- [x] 7.1 H-1: трактовать `CKR_CRYPTOKI_ALREADY_INITIALIZED` от `initialize()` как успех и класть контекст в реестр — иначе после `dlclose` модуля (libpam на `pam_end`) статики исчезают, а инициализированный провайдер остаётся, и в долгоживущем процессе `fly-dm` каждая следующая попытка входа получает `InitFailed` до перезагрузки машины
- [x] 7.2 Тест на H-1: смоделировать «провайдер уже инициализирован извне реестра» (например, инициализировать контекст мимо `load`, затем вызвать `load`) — вход не должен отвергаться
- [x] 7.3 M-1: при расхождении режимов побеждает более строгий (`mutex` над `os`), а не первый загрузивший; тест на апгрейд
- [x] 7.4 M-2: повторить `logout()` один раз при ошибке; при повторном отказе — ERROR с явным последствием («токен мог остаться залогиненным до конца жизни процесса»), а не технической формулировкой
- [x] 7.5 L-1: `os_mode_allows_concurrent_provider_calls` — добавить `assert_eq!(backend.locking_mode(), Os)`, иначе тест тихо прогонит сериализованный сценарий и останется зелёным
- [x] 7.6 L-3: пометить в докстрингах тесты, копирующие вендорский модуль, — на хосте, где копия грузится, они воспроизводят условие, роняющее процесс
- [x] 7.7 L-4: поправить «cryptoki 0.7» в `session.rs:4` и `:114` — крейт 0.12
- [x] 7.8 Прогон на живом Рутокене после правок
- [x] 7.10 Изоляция тестов по второму модулю: ветка с `PKCS11_MODULE_PATH_2` возвращала путь дословно в обход tempdir, поэтому все тесты, требующие «невиданного пути», делили один контекст — на живом Рутокене с двумя провайдерами набор был красный
- [x] 7.9 Третий круг ревью

## 8. Третий круг ревью

- [x] 8.1 H-1: `CKR_USER_ALREADY_LOGGED_IN` от `C_Login` → `C_Logout` + повторный `C_Login` с предъявленным PIN; НЕ трактовать как успех (это был бы вход по чужому логину). Тест: остаточный логин не ломает следующую попытку; неверный PIN при остаточном логине даёт `PinIncorrect`
- [x] 8.2 M-1: на ветке присвоения контекста поднимать режим до `Mutex` безусловно, независимо от конфига — чужие `CInitializeArgs` могли объявить провайдера однопоточным (`NULL pInitArgs`)
- [x] 8.3 M-3: конкурентный тест против вендорского провайдера сделать opt-in по отдельной переменной — его режим отказа `abort` бинаря без диагностики
- [x] 8.4 M-4: `fresh_module_path` не должен молча копировать вендорский модуль при отсутствии `PKCS11_MODULE_PATH_2` — два живых экземпляра одной библиотеки на одном устройстве это состояние, на которое `rtpkcs11ecp` отвечал `abort`; лучше skip
- [x] 8.5 L-1: `Acquire`/`Release` вместо `Relaxed` на флаге режима — тест утверждает ретроактивность как гарантию
- [x] 8.6 L-2: `C_CloseSession` на ветках отказа `C_Login` уходит вне `with_global_lock`, хотя `Drop` этого специально избегает
- [x] 8.7 L-3: `CKR_USER_NOT_LOGGED_IN` на ПЕРВОЙ попытке logout — уже успех, не уходить в повтор и не писать WARN
- [x] 8.8 L-4: поправить обоснование повтора logout — повтор мгновенный и под тем же мьютексом, «транзиентная конкуренция» им не лечится
- [x] 8.9 L-5: отдельный счётчик присвоений, чтобы тест утверждал положительный факт, а не отсутствие изменения счётчика
- [x] 8.10 L-6: убрать ложное `skipped:` в логе там, где тест продолжает выполняться
- [x] 8.11 M-2: читать режим внутри цикла в `find_slot`, а не один раз на весь перебор слотов
- [x] 8.12 Прогон на живом Рутокене и четвёртый круг ревью

## 9. Четвёртый круг ревью

- [x] 9.1 H-1: записать в канон вытеснение собственной живой сессии как отдельное направление, не покрытое принятой ценой Решения 7 (Risks + Open Question 6)
- [x] 9.2–9.8 вынесены в отдельный change `pkcs11-session-hardening`: счётчик живых логинов, маппинг `LogoutFailed` в средовый PAM-код, диагностика вытеснения, пересмотр запрета копирования модуля в тестах. Здесь закрыты — работа продолжается там.
