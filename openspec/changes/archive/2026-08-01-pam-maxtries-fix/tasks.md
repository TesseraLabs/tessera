# Tasks: pam-maxtries-fix

- [x] 1. `FlowError::pam_code()`: класс `MaxTries / PinLocked / MaxAttemptsExceeded` → `11`; в комментарии оставить имя константы
- [x] 2. Юнит-тест `flow.rs:2312` привести к `11`; пройтись по остальным утверждениям `pam_code()` и сверить каждое число с `<security/pam_appl.h>` — ошибка могла повториться
- [x] 3. Проверить, нет ли завязок на прежнее значение в `dist/pam.d/*`, `docs/` и хелперах
- [x] 4. Запись в `docs/ru/changelog.md` и английском переводе — исправление поведения, с указанием прежнего и нового кода
- [x] 5. Прогон e2e (`--profile ubuntu-container`): `AUTH-002` должен стать `PASS`
- [x] 6. Обновить `tests/e2e/BASELINE.md`: `AUTH-002` → `PASS`, комментарий заменить отсылкой к этому изменению
