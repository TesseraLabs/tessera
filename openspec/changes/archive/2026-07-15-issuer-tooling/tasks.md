# Tasks: issuer-tooling

## 1. Разделяемый модуль расширений (D2)

- [x] 1.1 Решить форму: крейт `tessera_ext` vs фичегейт/re-export из `tessera_core` (критерий: `tessera_issuer` не тянет PAM/libpdp); зафиксировать решение в design.md
- [x] 1.2 Вынести OID-константы и DER-схемы custom-расширений в разделяемый модуль; `tessera_core` переходит на него без изменения поведения (существующие тесты зелёные)
- [x] 1.3 Вынести предикат монотонного сужения рамок делегирования в тот же модуль; unit-тесты на сужение/равенство/расширение по каждому измерению рамок

## 2. Ядро tessera_issuer

- [x] 2.1 Скаффолд крейта `tessera_issuer` (lib + bin `issuer`) в workspace; ядро sync, без tokio; проверка сборки под wasm32-unknown-unknown (адаптеры за feature-флагами)
- [x] 2.2 Сборка TBS профиля «лист смены»: обязательные расширения, отказ до подписи при неполном наборе; unit-тесты
- [x] 2.3 Сборка TBS профиля «CA организации» с назначением рамок; unit-тесты
- [x] 2.4 Проверка монотонности рамок до подписи (разделяемый предикат из 1.3); тест: рамки шире родителя → отказ с указанием измерения
- [x] 2.5 Случайные 128-битные серийники (CSPRNG, положительное число, ≤ 20 октетов); тесты формата
- [x] 2.6 Выпуск CRL: состав из локального стейта, монотонный `crlNumber` со сверкой по журналу, отказ при откате; тесты
- [x] 2.7 Самопроверка артефакта после подписи разделяемым кодом валидации (контракт с Engine); contract-тест «что выпустил issuer — принял разбор Engine»
- [x] 2.8 Выпуск листа по CSR (D10): разбор PKCS#10 (x509-cert CertReq), верификация самоподписи до выпуска (P-256/RSA, pure Rust, wasm-совместимо), `issue_leaf_from_csr` поверх `issue_leaf`; тесты: валидный CSR, битая самоподпись → отказ до подписи, CSR-атрибуты не влияют на состав расширений

## 3. Подпись (issuer-signing)

- [x] 3.1 Trait «подписать готовый TBS ключом слота» (вход: байты TBS + id ключа; выход: подпись + алгоритм); mock-реализация для тестов ядра
- [x] 3.2 Адаптер PKCS#11 (cryptoki): токен и HSM одним кодом; PIN — запрос на время операции, zeroize, не в логах/аргументах; интеграционный тест на SoftHSM
- [x] 3.3 Адаптер Vault/OpenBao Transit (подпись готового TBS, PKI не используется); интеграционный тест против dev-server
- [x] 3.4 `issuer serve`: HTTP строго на 127.0.0.1, allowlist Origin, парный токен сессии, PIN только на стороне агента; тесты: чужой Origin → отказ до PKCS#11, без токена → отказ
- [x] 3.5 Подтверждение операции оператором в `issuer serve`: разбор TBS через `tessera_ext`, показ типа/subject/срока/рамок до обращения к модулю подписи; канал — pinentry (Assuan CONFIRM/GETPIN, прецедент gpg-agent) с fallback на терминал; инжектируемый confirmer для тестов; тесты: отказ оператора → backend не вызван; нечитаемый TBS → отказ до подтверждения; валидный токен без подтверждения не подписывает
- [x] 3.6 Режим демона (user-scope, кроссплатформенно — D11): pairing-токен файлом в пользовательском рантайм-каталоге платформы (Linux `$XDG_RUNTIME_DIR`, macOS `~/Library/Application Support`, Windows `%LOCALAPPDATA%`; unix-права 0600/0700 через `#[cfg(unix)]`); примеры автостарта: systemd --user unit, launchd plist, Task Scheduler; НЕ системный сервис; тесты: права/содержимое токен-файла, перезапись при рестарте
- [x] 3.7 Кроссплатформенность (D11): в `tessera_issuer` нет unix-only зависимостей вне `#[cfg(unix)]`; сборка и тесты проходят на macOS (локально) и Windows (CI, см. 7.5)
- [x] 3.8 Локализация RU/EN (D12): таблица строк в `tessera_issuer` (локаль: конфиг → TESSERA_ISSUER_LANG → LANG → EN); перевод подписей сводки подтверждения (pinentry/терминал), сообщений serve и пользовательского вывода CLI; технические идентификаторы не переводятся; тесты: ru-локаль → русские подписи при неизменных значениях, неизвестная локаль → EN

## 4. CLI

- [x] 4.1 Команды `issuer`: выпуск CA, выпуск листа, выпуск CRL, верификация журнала — тем же ядром, без переопределения проверок
- [x] 4.2 Тест паритета обёрток: одинаковый невалидный запрос через CLI и через ядро-напрямую даёт один и тот же отказ
- [x] 4.3 CLI: `issue-leaf --csr <file>` (PEM/DER), отображение subject и статуса самоподписи; тест: битый CSR → отказ
- [x] 4.4 CLI: `issuer csr` — генерация CSR стороной инженера ключом на его токене (PKCS#11-адаптер); самоподпись через тот же trait подписи
- [x] 4.5 Самоподписанный корень: `issue_root` в ядре (CA-профиль, issuer==subject, промоут test_support::self_signed_ca в продукт) + CLI `issue-root`; без него serverless-модель не бутстрапится; тесты: корень принимается inspect_parent как root, под ним проходит issue-ca

## 5. Журнал выпусков (issuance-journal)

- [x] 5.1 Hash-chain записи (NDJSON, монотонный seq, хэш предыдущей, фиксированный genesis); запись до выдачи артефакта, недоступность журнала → отказ операции; тесты
- [x] 5.2 Верификация цепочки: разрыв/подмена/переупорядочивание → позиция первой невалидной записи; тесты
- [x] 5.3 Подпись головы цепочки через trait подписи (по завершении сессии и по команде); статус «цела, хвост не подписан» vs «нарушена»; тесты

## 6. Веб-кабинет

- [x] 6.1 WASM-биндинги ядра (сборка TBS, проверки, журнал) + remote-адаптер подписи к `issuer serve`
- [x] 6.2 SPA-скаффолд: статика без внешних запросов (только 127.0.0.1), стейт — файлы через file picker/download; RU/EN словарь с переключателем и автовыбором по языку браузера (D12)
- [x] 6.3 Режим из родительского сертификата: корень → выпуск CA; CA → выпуск листов в рамках; лист/непригодный → операции недоступны с объяснением
- [x] 6.4 Снапшот инвентаря: проверка подписи (битая → отказ), пометка «подписанный/ручной» + возраст; ручное заполнение без снапшота
- [x] 6.5 E2E-прогон: полный цикл выпуска офлайн (кабинет + `issuer serve` + SoftHSM/токен) — бутстрап корня через issue-root на живом SoftHSM, выпуск CA организации через кабинет→агент→подпись→сборка→скачивание; выпущенный CA цепляется к корню (issuer=Fleet Root, CA:TRUE, рамки с ролью oper); нашёл 2 бага (CSP wasm-unsafe-eval, molчаливый провал init) — исправлены
- [x] 6.6 Кабинет: загрузка CSR как источника ключа листа — subject и статус самоподписи, prefill из CSR-атрибутов с пометкой «запрошено в CSR», битая самоподпись → выпуск недоступен

## 7. Сборка, CI, документация

- [x] 7.1 CI: wasm32-таргет, сборка SPA отдельным релизным артефактом (в deb агента не входит), фиксированные хэши артефакта в релизе; рассмотреть прогон mac-tests-гейченных сюит (включая issuer_contract) отдельным CI-шагом — сейчас они не исполняются в CI (предсуществующий пробел)
- [x] 7.2 Дополнение threat-model: поверхность локального агента (127.0.0.1, Origin, парный токен), обращение с PIN, целостность WASM-артефакта (ru+en §11; остаточный риск «SPA подсовывает иной TBS» → рекомендация превью TBS перед PIN)
- [x] 7.3 Документация: доки issuer в docs/ru + перевод в docs/en (выпуск через CLI, кабинет, Vault Transit, ограничение Vault PKI; запуск агента на Linux/macOS/Windows и автостарт)
- [x] 7.5 CI-матрица issuer: build+test `tessera_issuer`+`tessera_ext` на ubuntu/macos/windows-раннерах (только эти крейты — PAM/udev-часть workspace не для Windows)
- [x] 7.6 Деплой кабинета (D13): CI-workflow `deploy-issuer.yml` — на релизный тег собирает SPA, публикует на Cloudflare Pages (issuer.tessera-access.com) и в YC Object Storage+CDN (issuer.tessera-access.ru), фиксирует SHA-256 артефакта в релизе; purge CDN с учётом лимита 1/мин; секреты — repo secrets (CF token, YC keys); ручной деплой запрещён (паритет с deploy-docs)
- [x] 7.4 Обновить openspec/specs/README.md (индекс) после архивирования change
