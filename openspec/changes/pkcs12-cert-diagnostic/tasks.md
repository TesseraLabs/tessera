## 1. Красные тесты вперёд

- [ ] 1.1 Тест: из контейнера с зашифрованным ключевым мешком и незашифрованным `CertBag` сертификат извлекается без пароля (сейчас падает — это и есть дефект)
- [ ] 1.2 Тест: контейнер без незашифрованного `CertBag` даёт штатное «сертификат недоступен», а не ошибку
- [ ] 1.3 Тест: контейнер, собранный сторонним инструментом (`openssl` с `-certpbe NONE`), читается тем же путём — свойство не привязано к нашему инструменту сборки
- [ ] 1.4 Тест: битый контейнер даёт «недоступен», паники нет
- [ ] 1.5 Тест: ключевой материал этим путём не разбирается (структурно: разбор не касается `pkcs8ShroudedKeyBag`)

## 2. Реализация

- [ ] 2.1 Разбор `AuthenticatedSafe` с выборкой `CertBag` из разделов `id-data`, без обращения к ключевым мешкам
- [ ] 2.2 Выбор листа среди нескольких сертификатов раздела — тем же правилом, что применяет путь аутентификации, а не «первый попавшийся»
- [ ] 2.3 Прогнать тесты 1.1–1.5 — зелёные

## 3. Потребители

- [ ] 3.1 Проверить диагностику на экране входа: при исчерпании PIN показывается host/user-binding (`pam_tessera/src/flow.rs:2086`)
- [ ] 3.2 Проверить отчёт импорта: серийник непустой (`tessera_cli/src/enroll.rs:173`)
- [ ] 3.3 Убедиться, что путь аутентификации не изменился: `from_p12`, PIN-цикл и проверка цепочки нетронуты

## 4. Сквозная проверка

- [ ] 4.1 Прогон на стенде: неверный PIN на носителе с чужим удостоверением — диагностика называет, для какого устройства оно выпущено
- [ ] 4.2 Прогон на носителе старого образца — поведение прежнее, ошибки нет

## 5. Связанные критерии

- [ ] 5.1 Снять пометки в `openspec/changes/issuer-key-generation/tasks.md` (критерии 2.2 и часть 4.1), оставленные незакрытыми из-за этого дефекта
- [ ] 5.2 Проверить, не обещает ли документация диагностику там, где она всё ещё недоступна

## 6. Завершение

- [ ] 6.1 `openspec validate pkcs12-cert-diagnostic --strict`
- [ ] 6.2 Ревью изменений
