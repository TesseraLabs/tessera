> **Переработка 2026-08-19 (D20–D32).** Задачи ниже выполнены под прежнюю конструкцию
> (телефонный канал, оператор, гибридный nonce, статический ключ в `K`). Часть результата
> переживает пивот без изменений (каноническая сериализация, голден-векторы, паритет
> натив↔WASM, профили алгоритмов, правило «только строже»); остальное требует переделки —
> см. блок «Переработка под серверный флоу» в конце файла.

## 1. Каркас крейта

- [x] 1.1 Крейт `crates/tessera_codes_contract`: `no_std`-совместимое ядро либо явный отказ от него с обоснованием в README крейта; без зависимостей от `tessera_core`, без I/O
- [x] 1.2 Цель `wasm32-unknown-unknown` в CI: job `codes-contract` в `.github/workflows/issuer.yml` (нативные тесты + сборка под wasm32 + clippy обоих наборов фич)
- [x] 1.3 Затирание чувствительного материала (`Z`, `K`) при уничтожении — тип-обёртка, тест на отсутствие `Debug`-утечки

## 2. Каноническая сериализация

- [x] 2.1 Таблица полей и кодировка `canon(...)`: длина `u32` BE + байты, UTF-8 NFC, числа BE фиксированной ширины
- [x] 2.2 Голден-векторы в репозитории: вход, ожидаемые байты `canon`, ожидаемый `K` при заданном `Z`, ожидаемый код
- [x] 2.3 Тест «перестановка полей структуры не меняет байты» (структурная гарантия, не комментарий)
- [ ] 2.4 Прогон голден-векторов САМОЙ WASM-сборкой (сейчас в CI под wasm32 только компиляция): нужен headless-раннер — `wasm32-wasip1` + wasmtime либо node-обвязка. Осознанный долг: расхождение натив↔wasm поймается только после этого шага

## 3. Ключ и код

- [x] 3.1 `SharedSecret` снаружи + KDF с контекстом `№ ‖ эпоха ‖ hash(билета)`
- [x] 3.2 Trait DH с документированным требованием валидации публичной точки; тест-заглушка на контракт trait'а
- [x] 3.3 `truncate_N` без смещения на неполном диапазоне; тест равномерности на границах
- [x] 3.4 Константная по времени сверка кода; единственная ошибка сверки
- [x] 3.5 Тесты: смена эпохи разводит `K`; правка любого поля билета разводит `K`

## 4. Параметры и nonce

- [x] 4.1 Тип параметров парка с проверкой минимума при разборе (код ≥ 6, попыток ≤ 10; дефолты 8/5)
- [x] 4.2 Частичный порядок «строже»; тест на размен строгости (длиннее код + больше попыток ≠ усиление)
- [x] 4.3 Тип гибридного nonce: счётчик ‖ хвост, разбор и сборка, проверка ширины и алфавита хвоста
- [x] 4.4 Отдельная ошибка переполнения счётчика; тест на отсутствие переноса по модулю
- [x] 4.5 Алфавит кода параметром: десятичный дефолт, Crockford base32 опцией; тесты на исключённые символы

## 5. Форматы документов

- [x] 5.1 Билет оператора: тип, разбор, сериализация, проверка структуры и срока; рамки
      несут непустой перечень ролей, «все роли» — маркер `*`, отвергаемый рядом с именем
- [x] 5.2 Запись реестра устройства с PoP-подписью: тип, разбор, проверка обеих подписей через trait
- [x] 5.3 Квитанция о выдаче: тип, обязательное основание, композитное имя файла
- [x] 5.4 Challenge телефонного канала: сборка, разбор, контрольный символ номера (переиспользование валидатора `device-number-identity`)
- [x] 5.5 Строгий разбор: неизвестное поле, битая структура, недопустимое значение — ошибка; тесты на каждый класс

## 6. Профили алгоритма

- [x] 6.1 Профиль параметром: P-256, ГОСТ VKO, X25519
- [x] 6.2 Пометка ГОСТ-профиля неподтверждённым; отказ разбора без явного признака принятия риска
- [x] 6.3 Тест: конфигурация с ГОСТ без признака отвергается с указанием на гейт

## 7. Приёмка

- [ ] 7.1 E2E-кейсы в реестре `tests/e2e/cases/` на телефонный канал появляются вместе с первым потребителем (`qr-login-core`) — здесь фиксируется зависимость, кейсы едут там
- [x] 7.2 `cargo clippy --all-features` и дефолтная сборка чисты (обе, не только CI-набор)
- [x] 7.3 Голден-векторы описаны в README крейта как часть совместимости (`compat-policy`)

## Переработка под серверный флоу (2026-08-19, D20–D32)

- [x] R.1 Вывод `K` перевести на эфемерный KEM: `K = KDF(DH(эфемер устройства, ключ
      сервера), № ‖ hash(билета сервера))`; статический ключ устройства из вывода убрать,
      оставив ему подпись challenge. Голден-векторы перегенерировать
- [x] R.2 Канонизацию расширить полями `эпоха` и `личный номер инженера`; добавить кейс
      «код под чужим именем не сходится»
- [x] R.3 Удалить тип гибридного nonce со счётчиком и ошибку переполнения; ввести
      проверку ширины длинного CSPRNG-nonce по профилю. Вместе с типом сняты персист
      состояния попытки (счётчик выданных значений, множество потреблённых nonce,
      ожидающие попытки) и детекция отката — на устройстве и, каскадом, история
      счётчика у выдающей стороны (`counter::assess`, ledger, флаги обхода, код
      возврата 11)
- [x] R.3a Требование спеки `qr-login-method` про ЕДИНСТВЕННЫЙ слот попытки закрыто
      здесь же, внутри R.3, а не отдельной задачей: сняв персист, восемь ожидающих
      попыток стало негде держать. Попытка живёт в `StartedAttempt` и держит
      device-global замок каталога состояния всю свою жизнь, так что второй вход,
      пришедший, пока первый отвечают, получает отказ, а не собственную попытку.
      Записано отдельной строкой, чтобы требование не осталось ничьим
> **Известное состояние ветки после R.3–R.4, не подлежащее мержу в main.**
> Переписано после R.4, потому что R.4 контроль повторов НЕ вернула — она дала форматы,
> которыми его можно будет выразить, но не механизм.
> Вместе с историей счётчика у выдающей стороны исчез её собственный контроль повторов и
> континуальности выдач. Грант и status-token теперь существуют как документы: грант
> несёт подписанный запрос с challenge внутри (а значит nonce и эфемерную точку одной
> попытки), status-token привязан к nonce и digest'у авторизации — этого достаточно,
> чтобы повтор был выразим. Но выпускать гранты некому (сервера в репозитории нет) и
> проверять их на устройстве нечем (кабельный транспорт D21 не реализован), поэтому
> сегодня повтор по-прежнему ловит только устройство — тем, что живой попытки с этим
> nonce больше нет. Врезка снимается, когда появится сторона, которая гранты выпускает,
> и сторона, которая их проверяет; форматы к этому готовы.
> Отдельно: challenge подписан ключом устройства (R.10), и выдача отвергает всё, что не
> подписано зарегистрированным прибором. На двух e2e-кейсах это сказалось: CODE-015
> переписан на более ранний рубеж (подмена личного номера ломает подпись, и выдача
> отказывает раньше сверки), а CODE-014 сохранён как был, но требует от стенда двух вещей
> — бинаря раннера и файла `device-key.pem` в комплекте фикстур: подменённый challenge там
> подписывается заново, потому что у снявшего диск ключ устройства есть по условию задачи.
> Подробности — в `tests/e2e/BASELINE.md`.

- [x] R.4 Заменить формат квитинции оператора форматами: подписанный запрос инженера
      (единый канонический объект без дублей авторизационных полей), грант,
      status-token; билет оператора переименовать в билет сервера.
      Форматы добавлены (`request.rs`, `grant.rs`, `status.rs`), билет и адресат в
      challenge переименованы в сервер (`operator=` → `server=` в проводной форме обоих
      документов; канонические байты НЕ изменились — имя поля в них не пишется).
      Квитанция НЕ удалена — см. R.13: удалять её сейчас нечем заменить
- [x] R.5 Запись реестра устройства: разложить на `record_payload` + три подписи в
      фиксированном порядке (PoP, организация, владелец по digest предыдущего); добавить
      поля уровня защиты ключа и наличия монотонного якоря.
      Порядок подписей — свойство формата в двух слоях: фиксированные позиции в проводной
      форме (переставленные не разбираются) и вложенность сообщений (каждая подпись
      накрывает предыдущие, поэтому переставленный НАБОР не сходится). Лестница защиты
      ключа — по наблюдаемым признакам D31, четыре ступени, «не сообщил» отдельной
      ступенью. Якорь: наличие и тип, отсутствие — значение, а не ошибка.
      Добавлено поле `owner` — идентификатор заверяющего: спека перечисляет третью
      подпись, но не называет подписанта, а неназванного подписанта верификатору не
      разрешить по якорям
- [x] R.6 Добавить форматы записи и авторизации инженера. Два документа врозь: запись
      (кто есть и каким ключом подписывает) меняется редко, авторизация (что можно, где
      и до какого срока) — часто. В одном документе сужение прав означало бы перевыпуск
      утверждения о существовании человека, а правка рамок на месте была бы неотличима
      от того же действия злоумышленника. Отпечаток ключа считается в одном месте —
      `EngineerRecord::key_fingerprint`, — иначе авторизация назвала бы ключ, которого
      никто не держит
- [x] R.7 Ввести нормативную константу `MAX_ATTEMPT_TTL` и проверку `0 < ttl <= MAX`.
      Срок жизни попытки стал параметром парка (`FleetParams::attempt_ttl_secs`) и
      координатой частичного порядка «строже»: короче — строже. Ядро больше не держит
      своей копии срока — конфигурация подаёт его в параметры, а `[codes].code_ttl_seconds`
      проверяется тем же разбором. `MAX_ATTEMPT_TTL_SECS = 600`, дефолт 300
- [x] R.8 Сменить дефолтный алфавит на Crockford base32, десятичный оставить профилем;
      перекалибровать длину кода и предел попыток под клавиатурный ввод.
      Калибровка в БИТАХ: `MIN_CODE_ENTROPY_BITS = 20` выведен из предела попыток
      (`MAX_ATTEMPTS_PER_NONCE = 10` попыток против 2^20 значений — меньше одного шанса
      на сто тысяч). Дефолт: шесть символов base32 = 30 бит против 26.5 у прежних восьми
      десятичных знаков — короче набирать и строже. Предел попыток не менялся: порог
      считался против МАКСИМАЛЬНОГО бюджета, поэтому дефолт ниже него уже покрыт.
      Дефолт ширины nonce пересчитан тем же способом: 26 символов base32 = 130 бит.
      Следствие, о котором нужно знать: шесть десятичных знаков (19.9 бита) больше не
      проходят порог — прежний контрактный МИНИМУМ длины кода в десятичном парке стал
      невалиден. Дефолт (8 знаков) валиден и раньше, и теперь
- [x] R.9 Проверка побайтового равенства продублированных полей гранта. Грант несёт
      снаружи сводку (устройство, роль, уровень, личный номер), и разбор сверяет каждое
      её поле с подписанным объектом побайтово; негативный тест на каждый класс
      расхождения. Внутри подписанного запроса дублей нет структурно: авторизационные
      поля лежат в challenge, второй копии некуда положить
- [x] R.10 Подпись challenge статическим ключом устройства: формат, сборка, проверка
      выдающей стороной. Спека требует её прямым текстом («статический ключ… служит
      только для подписи challenge»), но ни одна задача блока её не покрывала — пробел
      найден при реализации R.1. Подделки кода отсутствие подписи не даёт (подменивший
      эфемерную точку получит код под свою пару, и устройство откажет), но происхождение
      challenge не было доказано ничем: выдающая сторона не могла отличить challenge
      устройства от сочинённого.
      Формат — отдельный документ `SignedChallenge` со своим префиксом проводной формы:
      неподписанный challenge, предъявленный там, где ждут подписанный, отвергается
      читателем, а не обнаруживается позже как «подписи не хватает». Подписывается
      помеченное сообщение (`tessera-codes/v1/challenge-signature` ‖ канонические байты),
      поэтому те же байты не выдать за другой документ контракта.
      Ключ проверки берётся ИЗ ЗАПИСИ РЕЕСТРА устройства: challenge не несёт открытого
      ключа вовсе — не потому, что забыли, а чтобы проверку нельзя было написать
      наоборот. Запись сверяется с challenge по номеру и эпохе, иначе довольно было бы
      подсунуть чужую запись вместе с чужой подписью.
      Проверка на выдаче стоит ПОСЛЕ проверки рамок билета: расхождение номера или эпохи
      выдача называет своими словами («прочитан номер X, в записи Y»), а подпись,
      проверяемая раньше, отвечала бы на ту же ситуацию «устройство этого не подписывало»
      — оба отказа верны, но действовать оператору помогает только первый.
      На устройстве подпись снимается ключом контейнера ECDSA/SHA-256 — тем алгоритмом,
      который выдающая сторона умеет проверять; ключ, которым так подписать нельзя,
      отказывает в самом начале попытки, до вопросов инженеру.
      Стенду добавлен инструмент `xtask codes-sign-challenge` и файл `device-key.pem` в
      комплекте фикстур: кейс CODE-014 моделирует снятый диск, у такого атакующего ключ
      устройства есть, и отказ обязан приходить из сверки кода, а не из подписи. Байты
      сообщения инструмент берёт у крейта контракта, а не собирает сам — иначе кейс
      проверял бы совпадение двух написаний формата
- [x] R.12 Личный номер инженера в журнал устройства: сегодня он входит в канонизацию,
      но в записи `emit_denied`/`emit_success` не попадает. Сверка «входы на устройствах ↔
      выдачи на сервере» — то, ради чего номер и заведён, — по журналу устройства без него
      не собирается.
      Поле называется `claimed_engineer_no` — заявленное, а не установленное: реестра
      людей на офлайн-устройстве нет, устройство номер ни с чем не сверяет, и имя поля
      говорит об этом раньше, чем кто-нибудь прочтёт докблок. Номер попадает во все три
      события (`denied`, `attempts_exhausted`, `success`) и в хеш-цепочку; там, где
      отказ случился до промпта, пишется `-`, а не выдуманное значение.
      Заодно `emit_denied` переведён с семи позиционных аргументов на структуру
      `Denial`: два соседних `Option<&str>` — форма, в которой переставленные местами
      номер билета и номер инженера не заметил бы никто
- [ ] R.13 Снять формат квитанции оператора и обвязку выдачи вокруг него. Спека
      объявляет формат УДАЛЁННЫМ, но в ветке он пока живёт: грант выпускает сервер,
      которого в репозитории нет, а `issuer codes issue` собрать грант не может — у него
      нет ни ключа инженера (подпись делается на стороне инженера), ни подтверждающего,
      ни status-службы. Снести квитанцию сейчас значит оставить выдачу без пути вообще,
      а не заменить формат форматом. Снимается вместе с приходом серверной стороны;
      направление задано D24 и R.1 в tessera-enterprise (кабинет оператора уходит как
      продукт), порядок снятия — инженерный: сначала замена, потом удаление.
      Объём на сегодня: contract/receipt.rs, issuer/codes/{annex,store,issue,reconcile}.rs,
      подкоманды CLI `issue` / `receipts verify` / `reconcile`, сюита e2e
      `31-codes-reconcile` — ≈3400 строк
- [x] R.11 Тест на затирание приватной половины эфемерной пары — в той части, в какой
      это вообще проверяемо, и с явно записанным пределом там, где нет.
      Проверено механически: пара не клонируется (`compile_fail`-доктест, проверен
      мутацией — с `#[derive(Clone)]` доктест перестаёт падать), попытка тоже не
      клонируется, приватная половина не достаётся ни одним accessor'ом, пара порождается
      заново на каждую попытку.
      НЕ проверено и записано пределом рядом с кодом: что байты приватной половины
      действительно перезаписываются при уничтожении. Это происходит внутри библиотеки,
      которая освобождает ключ вместе с затиранием; после освобождения память принадлежит
      аллокатору, читать её — UB, и тест читал бы аллокатор, а не затирание. Что сдвинуло
      бы предел в проверку: держать скаляр своим `Zeroizing`-буфером и вести обмен по нему
      — другая конструкция, отдельное решение: сегодня оно опирается на
      освобождение ключа библиотекой и ничем не подпёрто — а это единственный секрет,
      существование которого ограничено одной попыткой

## Заведено по ревью переработки (2026-08-21), в эту волну не входит

Разбор ревью разложил находки на три корзины; ниже — те, что требуют отдельной работы или
решения владельца. Исправленные в волне здесь не повторяются: они в коде и в отчётах.

- [ ] V.1 Политика прав каталога состояния проверяется ПОСЛЕ чтения из него (M3).
      `open_inner` при `privileged = true` создаёт `state_dir` и читает эпоху раньше, чем
      зовёт `check_trusted`. Итоговый отказ наступает, но до него от root выполняются
      `mkdir -p` и чтение по пути, который политика ещё не признала доверенным. Порядок
      обратен тому, ради чего `open_privileged` отделён от `open`. Перестановка задевает
      чтение эпохи и порядок отказов — нужен тест на каждый порядок, поэтому отдельно
- [ ] V.2 Кто сверяет, что `server_id` гранта — та выдающая сторона, которую ждали (M4,
      вторая половина). В контракте это невыразимо: имя разрешается в ключ верификатором,
      а какие имена парк ждёт — вопрос якорей серверной стороны. Едет вместе с сервером
- [ ] V.3 Собственный pre-merge гейт change'а `codes-device-artifacts` (`threat-model` +
      `vuln-scan` для поверхности разбора носителя) не отработал (L6). Решение владельца:
      закрыть гейт или явно принять риск — но не проехать молча
- [ ] V.4 Голден-векторы контракта не гоняются самой WASM-сборкой (L7). Крейт написан
      ради того, чтобы кабинет в браузере считал те же байты, что устройство; расхождение
      натив↔wasm в замороженном формате сегодня не поймается ничем, а стоить будет «код не
      подходит» на объекте. Поднять гейт до слияния или явно принять риск
- [ ] V.5 Порог стойкости кода не сверяется с бюджетом попыток (L8). Парк может взять
      минимум энтропии и максимум попыток одновременно; `residual_risk` эту величину уже
      считает, но она справка, а не гейт. Сделать её гейтом — изменение политики, которое
      может отвергнуть уже разложенные конфигурации: решение владельца
- [ ] V.6 Условие «файл несёт обрамление» может отвергать чужие плоские выгрузки целиком
      (N4). После починки H3 достаточно одной строки с полем `seq` — обычным в выгрузках
      сборщиков логов, — чтобы файл целиком ушёл в проверку цепочки и вернулся
      `BrokenChain`. Направление безопасное (отказ, а не ложная зелень) и на родном формате
      не воспроизводится, но если плоская выгрузка — поддерживаемый вход, условие стоит
      привязать к обоим полям сразу либо к доле строк
- [ ] V.7 В строке отчёта сверки `refusals-read` зияет прогон из 18 пробелов (N5).
      Печатный артефакт продукта: `rustfmt` содержимое строк не трогает, тесты цепляются
      за префикс. Правка — одна строка
- [ ] V.8 `authenticate-mistyping-once` не проверяет, что бюджета попыток хватает на два
      ответа (N6). При `TESSERA_E2E_CODE_ATTEMPTS=1` верный код не будет подан вовсе, и
      REC-005 упадёт по причине, не имеющей отношения к проверяемой гарантии. Достаточно
      проверки в начале ветки

