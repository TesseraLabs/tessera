## 1. Каркас крейта

- [x] 1.1 Крейт `crates/tessera_codes_contract`: `no_std`-совместимое ядро либо явный отказ от него с обоснованием в README крейта; без зависимостей от `tessera_core`, без I/O
- [x] 1.2 Цель `wasm32-unknown-unknown` в CI: job `codes-contract` в `.github/workflows/issuer.yml` (нативные тесты + сборка под wasm32 + clippy обоих наборов фич)
- [x] 1.3 Затирание чувствительного материала (`Z`, `K`) при уничтожении — тип-обёртка, тест на отсутствие `Debug`-утечки

## 2. Каноническая сериализация

- [x] 2.1 Таблица полей и кодировка `canon(...)`: длина `u32` BE + байты, UTF-8 NFC, числа BE фиксированной ширины
- [x] 2.2 Голден-векторы в репозитории: вход, ожидаемые байты `canon`, ожидаемый `K` при заданном `Z`, ожидаемый код
- [x] 2.3 Тест «перестановка полей структуры не меняет байты» (структурная гарантия, не комментарий)
- [ ] 2.4 Прогон голден-векторов САМОЙ WASM-сборкой (сейчас в CI под wasm32 только компиляция): нужен headless-раннер — `wasm32-wasip1` + wasmtime либо node-обвязка. Осознанный долг: расхождение натив↔wasm поймается только после этого шага

## 3. Ключ и код

- [x] 3.1 `SharedSecret` снаружи + KDF с контекстом `№ ‖ эпоха ‖ hash(билета)`
- [x] 3.2 Trait DH с документированным требованием валидации публичной точки; тест-заглушка на контракт trait'а
- [x] 3.3 `truncate_N` без смещения на неполном диапазоне; тест равномерности на границах
- [x] 3.4 Константная по времени сверка кода; единственная ошибка сверки
- [x] 3.5 Тесты: смена эпохи разводит `K`; правка любого поля билета разводит `K`

## 4. Параметры и nonce

- [x] 4.1 Тип параметров парка с проверкой минимума при разборе (код ≥ 6, попыток ≤ 10; дефолты 8/5)
- [x] 4.2 Частичный порядок «строже»; тест на размен строгости (длиннее код + больше попыток ≠ усиление)
- [x] 4.3 Тип гибридного nonce: счётчик ‖ хвост, разбор и сборка, проверка ширины и алфавита хвоста
- [x] 4.4 Отдельная ошибка переполнения счётчика; тест на отсутствие переноса по модулю
- [x] 4.5 Алфавит кода параметром: десятичный дефолт, Crockford base32 опцией; тесты на исключённые символы

## 5. Форматы документов

- [x] 5.1 Билет оператора: тип, разбор, сериализация, проверка структуры и срока; рамки
      несут непустой перечень ролей, «все роли» — маркер `*`, отвергаемый рядом с именем
- [x] 5.2 Запись реестра устройства с PoP-подписью: тип, разбор, проверка обеих подписей через trait
- [x] 5.3 Квитанция о выдаче: тип, обязательное основание, композитное имя файла
- [x] 5.4 Challenge телефонного канала: сборка, разбор, контрольный символ номера (переиспользование валидатора `device-number-identity`)
- [x] 5.5 Строгий разбор: неизвестное поле, битая структура, недопустимое значение — ошибка; тесты на каждый класс

## 6. Профили алгоритма

- [x] 6.1 Профиль параметром: P-256, ГОСТ VKO, X25519
- [x] 6.2 Пометка ГОСТ-профиля неподтверждённым; отказ разбора без явного признака принятия риска
- [x] 6.3 Тест: конфигурация с ГОСТ без признака отвергается с указанием на гейт

## 7. Приёмка

- [ ] 7.1 E2E-кейсы в реестре `tests/e2e/cases/` на телефонный канал появляются вместе с первым потребителем (`qr-login-core`) — здесь фиксируется зависимость, кейсы едут там
- [x] 7.2 `cargo clippy --all-features` и дефолтная сборка чисты (обе, не только CI-набор)
- [x] 7.3 Голден-векторы описаны в README крейта как часть совместимости (`compat-policy`)
