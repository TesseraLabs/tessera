## 1. Нейтрализация в рендерере

- [x] 1.1 Ввести в `summary.rs` предикат `needs_neutralizing(c)` — закрытое перечисление: bidi-контроли (текущий `is_bidi_control`), C0 `U+0000..U+001F`, `DEL` `U+007F`, C1 `U+0080..U+009F`, `U+2028`, `U+2029`
- [x] 1.2 Переименовать `neutralize_bidi` в `neutralize_for_display` и перевести на новый предикат; вызовы в `render` обновить
- [x] 1.3 Обновить doc-комментарии `is_bidi_control` и функции нейтрализации: они больше не только про Trojan Source, но и про сокрытие строк
- [x] 1.4 Замаскировать ниббл в `hex_upper_nibble` (`summary.rs`) — `& 0xF` в точке вызова плюс `debug_assert!(nibble < 16)`

## 2. Тот же хвост в канале подтверждения

- [x] 2.1 Замаскировать ниббл в `hex_nibble` (`confirm.rs`) тем же способом
- [x] 2.2 Проверить, что doc-комментарий `assuan_escape` не обещает защиты отображения — экранирование обратимо и защищает только транспорт; при необходимости указать, где живёт нейтрализация

## 3. Тесты

- [x] 3.1 Тест: `U+0000` в значении роли отображается маркером, строки после него сохраняются (сценарий «NUL в значении не обрывает диалог»)
- [x] 3.2 Тест: `U+000A` и `U+2028` в значении не увеличивают число строк `render` (сценарий «Перенос строки не подделывает строку сводки»)
- [x] 3.3 Тест: значение из кириллицы, латиницы и пунктуации проходит через `render` байт-в-байт (сценарий «Обычные значения не искажаются»)
- [x] 3.4 Тест: для каждого символа закрытого множества маркер состоит только из `\u` и `[0-9A-F]{4}` — то есть сам не подлежит нейтрализации (сценарий «Маркер строится только из шестнадцатеричных цифр»)
- [x] 3.5 Тест на C1 (`U+0085` как минимум) — он проходит `is_control`, но не виден глазом в терминале
- [x] 3.6 Тест границ: символы, стоящие вплотную к нейтрализуемым диапазонам (`U+0020`, `U+007E`, `U+00A0`, `U+200D`, `U+2010`, `U+2027`, `U+202F`, `U+2065`, `U+206A`), проходят байт-в-байт — ловит сдвиг границы диапазона на единицу, по которой остальные четыре теста бьют серединой. Красной фазы, как и у 3.3, нет по построению

## 3a. Хвосты ревью

- [x] 3a.1 Дополнить `is_bidi_control` до полного `Bidi_Control=Yes`: не хватало `U+061C` (ALM) — он `Cf`, а не `Cc`, поэтому мимо любой проверки по категории; в `neutralized_set()` теста и в граничный тест (`U+061B`, `U+061D`) добавлено
- [x] 3a.2 Привести комментарии о маскировании ниббла (`summary.rs`, `confirm.rs`) в соответствие коду: `map_or('0', …)` не удалён, а стал недостижим — так и написано, вместе с причиной не заменять его паникой на пути подписи
- [x] 3a.3 Записать в Non-Goals `design.md`, что гомографы и невидимые `Cf` не закрываются и почему (списком кодовых точек этот класс не закрывается), с остаточным риском

## 4. Контур и проверки

- [x] 4.1 Записать в `tests/e2e/.e2e-ack` причину отсутствия e2e-кейса: у `Confirmer` нет потребителей в открытом дереве, диалог поднимает `issuer-agent` из закрытого репозитория, CLI `issuer` обращается к pinentry только за паролем
- [x] 4.2 `cargo test -p tessera_issuer --all-features`
- [x] 4.3 `cargo clippy --all-features` и `cargo clippy` без фич — CI линтит только первое
- [x] 4.4 Проверить, что `wasm32`-сборка `summary.rs` не сломана: модуль питает кабинет и не должен приобрести зависимостей
