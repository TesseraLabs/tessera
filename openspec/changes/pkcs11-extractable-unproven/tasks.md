## 1. Три состояния атрибута

- [x] 1.1 Тест: атрибут отсутствует в результате чтения → состояние «не сообщён», а не `FALSE`
- [x] 1.2 Тест: `CKA_KEY_TYPE` пришёл, `CKA_EXTRACTABLE` потерян — отличается от случая, когда потеряны оба (`KeyTypeAttributeMissing`)
- [x] 1.3 Перечисление состояний вместо булева в парсере атрибутов и в `FoundPrivateKey`
- [x] 1.4 Отдельный вариант ошибки `ExtractableAttributeUnavailable` с причиной отказа и hex-префиксом `CKA_ID`

## 2. Классификация отказа и перечитывание

- [x] 2.1 Тест: каждая ветка классификатора причин (`sensitive`, `type_invalid`, `unavailable`, противоречие, сбой запроса)
- [x] 2.2 Тест: уточняющий запрос вызывается только на холодном пути и не трогается при подтверждённых `FALSE`/`TRUE`
- [x] 2.3 Тест: уточняющий запрос сообщил «читаемо» → одиночное перечитывание, и его результат решает исход (`FALSE` → допуск, `TRUE` → `ExtractableKeyRejected`, пусто → `ExtractableAttributeUnavailable`)
- [x] 2.4 Чистая функция классификации + тонкая FFI-обёртка под process-global локом
- [x] 2.5 Проверить, что уточняющий запрос и перечитывание идут через `with_global_lock` и не входят в лок повторно
- [x] 2.6 Сбой самого перечитывания отражается причиной «сбой запроса», а не «читаемо, но не отдано»
- [x] 2.7 `Resolved` не может нести «не сообщено» — состояние сделано непредставимым, выдуманная причина по умолчанию убрана

## 3. Два ключа обхода

- [x] 3.1 Тест: `pkcs11_allow_unreported_extractable = true` разрешает неотвеченный атрибут и НЕ разрешает подтверждённое `TRUE`
- [x] 3.2 Тест: `pkcs11_allow_extractable_keys = true` разрешает подтверждённое `TRUE` и НЕ разрешает неотвеченный атрибут
- [x] 3.3 Новый ключ в `raw.rs` + валидация + проброс до `find_private_key_for_cert` по всем слоям
- [x] 3.4 WARN `pkcs11_extractable_attribute_unavailable` с полем причины, `target: "tessera.pkcs11"`, без сырых байт `CKA_ID`
- [x] 3.5 Каждый отказ называет в сообщении СВОЙ ключ обхода; тест двусторонний (свой ключ есть, чужого нет)

## 4. Документация и канон

- [x] 4.1 `docs/ru/configuration.md` и `docs/en/configuration.md`: новый ключ, сужение области действия старого, оба WARN-события
- [x] 4.2 `docs/ru/changelog.md`: слом поведения для молчащих провайдеров с названным средством; слом публичного API `FoundPrivateKey::extractable`
- [x] 4.3 `docs/ru/threat-model.md` и `docs/en/threat-model.md`: строка T2 перестаёт ссылаться только на `ExtractableKeyRejected`
- [x] 4.4 Таблица соответствия PAM-кодов в `pam_tessera` (док-комментарий) называет обе ошибки; сам `pam_code` по-прежнему отдаёт `PAM_AUTH_ERR` через общую ветку `Pkcs11(_)` — код тот же (7), явные ветки не заводились
- [x] 4.5 `openspec/specs/README.md`: односточное описание требования перестаёт быть двухсостоянийным

## 5. Проверка

- [x] 5.1 `cargo test -p tessera_core`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --check`
- [x] 5.2 Прогон `pkcs11_singleton`, `pkcs11_integration`, `pkcs11_cert_lookup`, `pkcs11_smoke` на живом токене — режим B на Рутокен ЭЦП 3.0 не изменился (22 теста, 2026-08-02, финальное состояние кода)
- [x] 5.3 `openspec validate pkcs11-extractable-unproven --strict`

## 6. Осталось на живом железе

Токен отключён и недоступен; при следующем подключении Рутокен ЭЦП 3.0:

- [ ] 6.1 Провести ключ через полный auth-путь `find_private_key_for_cert` — сейчас на токене нет объекта сертификата, поэтому сквозной прогон невозможен, проверялось чтением атрибутов напрямую
- [ ] 6.2 ГОСТ-трек (`gost-pkcs11`) — отдельная задача, механизмы на токене подтверждены
