## 1. Предусловие

- [ ] 1.1 Убедиться, что change `pkcs11-init-singleton` реализован и смержен — он меняет владение контекстом в `backend.rs`, поверх которого ложится чтение объектов данных

## 2. Чтение объектов данных (красные тесты вперёд)

- [ ] 2.1 Тест: поиск `CKO_DATA` по метке возвращает байты `CKA_VALUE` и флаг `CKA_PRIVATE` (live, гейт `PKCS11_MODULE_PATH`)
- [ ] 2.2 Тест: отсутствующая метка даёт ошибку «не найдено», отличимую от ошибки чтения
- [ ] 2.3 Тест: объект с `CKA_PRIVATE = false` отвергается вызывающим кодом
- [ ] 2.4 Реализовать модуль чтения объектов данных в `token/pkcs11/`
- [ ] 2.5 Прогнать тесты 2.1–2.3 на живом пассивном токене — зелёные

## 3. Источник конверта в auth-пути

- [ ] 3.1 Тест: при отсутствии ключа источника в конфиге применяется раздел USB-носителя (поведение не изменилось)
- [ ] 3.2 Тест: при источнике «токен» mass-storage не задействуется — нет `wait_for_usb` и монтирования
- [ ] 3.3 Тест: после получения конверта порядок проверок одинаков для обоих источников
- [ ] 3.4 Тест: при источнике «токен» `AuthContext.usb_serial` содержит serial токена, а не пусто — иначе monitord не сматчит извлечение и сессия переживёт вынутый носитель
- [ ] 3.5 Тест: пустой `CK_TOKEN_INFO.serialNumber` при источнике «токен» даёт `TokenSerialMissing` и прерывает аутентификацию
- [ ] 3.6 Добавить ключ выбора источника в `config/raw.rs` и `config/validated.rs` с дефолтом «раздел USB-носителя»
- [ ] 3.7 Развести получение конверта в `authenticate_pkcs12` (flow.rs:430–762), не трогая ничего после конверта; идентификатор носителя брать из фактического устройства — serial токена при источнике «токен»
- [ ] 3.8 Прогнать тесты 3.1–3.5 — зелёные

## 4. Запись конверта инструментом выпуска

- [ ] 4.1 Тест: запись объекта, превышающего лимит устройства, отвергается до обращения к токену
- [ ] 4.2 Тест: расхождение перечитанного объекта с исходником трактуется как неуспешная запись
- [ ] 4.3 Реализовать запись конверта на токен: `CKA_PRIVATE = true`, проверка размера, обязательная верификация чтением
- [ ] 4.4 Проверить на живом Рутокен Lite: конверт 2–3 КБ пишется и читается, объект 48 КБ отвергается (а не усекается молча)

## 5. Документация и границы

- [ ] 5.1 Описать носитель в `docs/ru` и `docs/en`: как записать конверт, требование приватности объекта, лимит размера
- [ ] 5.2 Явно зафиксировать уровень доверия — «съёмный носитель плюс PIN», ключ извлекаем, подпись на хосте; проверить, что формулировок об аппаратной защите ключа нет
- [ ] 5.3 Свериться с маркетинговыми материалами (`docs/marketing/`) — не появилось ли обещаний, которых этот носитель не даёт
- [ ] 5.4 Записать в runbook живой проверки: сценарий пассивного токена и обязательная сверка round-trip
- [ ] 5.5 `master-code-reviewer` по всему диффу; CRITICAL/HIGH закрыть до мержа
