## 1. Предусловие

- [x] 1.1 Убедиться, что change `pkcs11-init-singleton` реализован и смержен — он меняет владение контекстом в `backend.rs`, поверх которого ложится чтение объектов данных — процесс-глобальный реестр контекстов с adopt при `CKR_CRYPTOKI_ALREADY_INITIALIZED` присутствует в `token/pkcs11/backend.rs` базы ветки

## 2. Чтение объектов данных (красные тесты вперёд)

- [x] 2.1 Тест: поиск `CKO_DATA` по метке возвращает байты `CKA_VALUE` и флаг `CKA_PRIVATE` (live, гейт `PKCS11_MODULE_PATH`) — `tests/pkcs11_data_object.rs::live_container_survives_the_round_trip`
- [x] 2.2 Тест: отсутствующая метка даёт ошибку «не найдено», отличимую от ошибки чтения — `live_absent_label_is_not_found` (варианты `DataObjectNotFound` и `DataObjectUnreadable`) плюс юнит-тесты `data_object::tests`
- [x] 2.3 Тест: объект с `CKA_PRIVATE = false` отвергается вызывающим кодом — `live_public_object_is_refused_by_the_reading_path`: объект создаётся публичным напрямую через cryptoki (наш писатель такой не делает), `read_private_data_object` даёт `DataObjectNotPrivate`
- [x] 2.4 Реализовать модуль чтения объектов данных в `token/pkcs11/` — `token/pkcs11/data_object.rs`: `find_data_object` (байты + флаг приватности) и `read_private_data_object` (отвергает публичный)
- [x] 2.5 Прогнать тесты 2.1–2.3 на живом пассивном токене — зелёные — Рутокен Lite `483d4e1a`, 5/5 passed

## 3. Источник конверта в auth-пути

- [x] 3.1 Тест: при отсутствии ключа источника в конфиге применяется раздел USB-носителя (поведение не изменилось) — `config::validated::tests::an_absent_source_key_is_the_usb_partition` (дефолт валидатора) и `flow::tests::an_absent_source_key_still_reads_the_usb_partition` (полный прогон: `wait_for_usb` и монтирование по-прежнему происходят, serial USB на месте); краснота проверена перестановкой `#[default]` на `TokenObject`
- [x] 3.2 Тест: при источнике «токен» mass-storage не задействуется — нет `wait_for_usb` и монтирования — `flow::tests::the_token_carrier_never_touches_mass_storage`: счётчики `InMemoryFlowIo::usb_waits`/`mounts` равны нулю, `FlowOutcome.mount` пуст; краснота проверена подменой ветки токена на `usb_envelope`
- [x] 3.3 Тест: после получения конверта порядок проверок одинаков для обоих источников — `flow::tests::both_carriers_run_the_same_checks_after_the_envelope` (один и тот же конверт даёт совпадающие cert/role/MAC-поля и одинаковую последовательность приглашений PIN) и `both_carriers_fail_the_same_way_after_the_envelope` (неверный PIN — `MaxTries` после ровно трёх попыток на обоих; чужой якорь — `Trust` с одним PAM-кодом); краснота проверена подменой конверта у токена на другой фикстурный `.p12`
- [x] 3.4 Тест: при источнике «токен» `AuthContext.usb_serial` содержит serial токена, а не пусто — `flow::tests::the_token_carrier_records_the_token_serial_for_removal_enforcement`: проверяется и `AuthContext`, и то, что `RecordingMonitor` получил тот же serial без VID/PID и devnode; краснота проверена заменой `carrier_serial` на `None`
- [x] 3.5 Тест: пустой `CK_TOKEN_INFO.serialNumber` при источнике «токен» даёт `TokenSerialMissing` и прерывает аутентификацию — `flow::tests::a_token_without_a_serial_is_refused_before_the_pin_prompt` (serial читается ДО приглашения PIN: счётчик приглашений равен нулю) и `a_serial_less_token_stops_the_authentication` (сессия у monitord не открывается, отката к mass-storage нет); краснота проверена переносом чтения serial после `acquire_session`
- [x] 3.6 Добавить ключ выбора источника в `config/raw.rs` и `config/validated.rs` с дефолтом «раздел USB-носителя» — `pkcs12_source = "usb_partition" | "token_object"` (дефолт `usb_partition`) плюс `pkcs12_token_object_label`; в валидированном виде это `Pkcs12Source::TokenObject { object_label }`, так что «токен без метки» непредставим. Метка без токенного источника, токенный источник в `mode = "pkcs11"` и токенный источник без `pkcs11_module` отвергаются (молча проигнорированный ключ — та же ошибка, что 4.12); предел метки 128 Б совпадает с тем, что пишет инструмент выпуска
- [x] 3.7 Развести получение конверта в `authenticate_pkcs12`, не трогая ничего после конверта; идентификатор носителя брать из фактического устройства — USB-ветка вынесена в `usb_envelope` без изменений, добавлена `token_envelope`; обе возвращают `CarriedEnvelope`, дальше идёт прежний общий код. Чтение — `read_private_data_object` (публичный объект отвергается), сессия PKCS#11 закрывается с `C_Logout` через `Drop` на всех путях, включая ошибочные; свой `C_Initialize` не заводится — используется `real_pkcs11_io`. Дополнительно: при токенном источнике `ensure_pkcs11_presence_mode` отвергает `monitor_fail_mode = strict` (демон слушает подсистему `block`, CCID-токен туда не попадает — иначе строгий режим молча не выполнял бы своё обещание), тест `strict_monitoring_is_refused_for_the_token_carrier`
- [x] 3.8 Прогнать тесты 3.1–3.5 — зелёные — `cargo test --workspace`: 1603 passed, 0 failed; `cargo clippy --workspace --all-targets --all-features -- -D warnings` и `cargo fmt --all --check` чисты. Живая проверка на Рутокен ECP (serial `48b541ca`): конверт 3622 Б записан `issuer prepare-carrier`, прочитан кодом auth-пути побайтово идентично — `tests/pkcs12_token_carrier_live.rs::live_the_carrier_object_reaches_the_authentication_path`

- [x] 3.10 Носитель выбирается конфигурацией, а не порядком перечисления слотов — при нескольких подключённых токенах, подходящих под `pkcs11_token_label` (в том числе когда метка не задана и подходят все), чтение отвергается с `FlowError::TokenCarrierAmbiguous`. Перебор слотов отвергнут: чтение приватного объекта требует логина, то есть перебор предъявлял бы PIN токенам, которым он не предназначен, и жёг бы их счётчики попыток. `Pkcs11Backend::find_slots` (ядро), гард в `read_token_carrier_with`; тесты `two_matching_tokens_are_refused_before_the_pin_prompt`, `the_ambiguity_refusal_names_the_key_that_resolves_it`, `a_single_matching_token_is_read_without_complaint`; краснота проверена отключением гарда. Живой прогон на двух подключённых токенах — отказ с диагностикой; с явным `TESSERA_CARRIER_TOKEN` читается именно названный (serial `483d4e1a`), при названии другого токена — `DataObjectNotFound`, а не подбор

- [x] 3.11 Живой тест выбирает токен явно — `TESSERA_CARRIER_TOKEN` → `cfg.pkcs11_token_label`, плюс необязательная сверка `TESSERA_CARRIER_TOKEN_SERIAL` (иначе чтение с чужого токена выглядит как несовпадение байт и уводит к железу вместо конфигурации). Добавлен самоопределяющийся живой тест `live_an_unnamed_carrier_is_refused_when_several_tokens_are_connected` — на стенде с одним токеном пропускается

- [x] 3.12 (ревью) Токен для чтения берётся из проверенного набора, а не из порядка перечисления — `read_token_carrier_with` использует единственный элемент `matching_tokens`, `wait_for_token` остался только ожиданием появления; нуль/один/больше одного покрыты одним `match` (исчезнувший между вызовами токен → `NoTokenAvailable`, а не стухший слот в PIN-приглашении). Стаб переработан так, чтобы слот прибытия и проверенный набор могли разойтись, и записывает слоты, на которых шла работа. Тесты `the_token_used_is_the_one_the_check_passed`, `a_token_that_vanished_between_the_wait_and_the_check_is_not_used`; краснота проверена обоими способами — возвратом слота прибытия и снятием ветки нуля

- [x] 3.13 (ревью) `DataObject*` дают тот же PAM-код, что и отсутствие удостоверения на флешке — 9 (`PAM_AUTHINFO_UNAVAIL`) для `DataObjectNotFound`/`NotPrivate`/`Unreadable`/`Ambiguous`. На USB те же ситуации уже отвечали девяткой (`Discovery`, `P12Envelope`), а семёрка в стеке с `authinfo_unavail=ignore` означала бы отказ во входе там, где другой носитель даёт откат. Таблица в докстринге дополнена; тест `a_carrier_without_a_usable_credential_answers_alike_on_both_media` сверяет коды напрямую с USB-веткой; краснота проверена

- [x] 3.14 (ревью, часть 1) Нечитаемый сосед под той же меткой отвергает поиск целиком — выбор кандидата вынесен в чистую `choose_data_object`, `first_failure` проверяется до выдачи любого кандидата. Прежний код игнорировал его в ОБЕИХ ветках (и приватной, и публичной), пока кандидат нашёлся, — то есть гарантия «метка указывает на одно удостоверение» держалась на удаче провайдера. Тесты `an_unreadable_sibling_refuses_a_readable_private_object`, `an_unreadable_sibling_refuses_a_public_object_too`, плюс три на неизменившиеся ветки; краснота проверена возвратом прежнего порядка (оба теста FAILED)

- [x] 3.15 (ревью) Тест различия по цепочке — `only_the_usb_carrier_adds_intermediates_from_beside_the_container`: записывающий `Stage2TrustVerifier` фиксирует, сколько промежуточных дошло до проверки, и USB с `certs/chain.pem` обязан предъявить больше, чем токен (2 против 1). Это единственное место, где вход в проверку зависит от носителя, и различие идёт в строгую сторону — промежуточных меньше, а не больше. Заодно тест равенства `both_carriers_run_the_same_checks_after_the_envelope` переведён на фикстуру С `chain.pem`, чтобы не обходить это различие выбором фикстуры. Краснота проверена выдачей `chain_pem` на токенной ветке — `usb presented [2], token presented [2]`, FAILED

- [x] 3.16 (ревью) `pkcs12_path_pattern` при `token_object` отвергается — у токена нет файловой системы, и принятый ключ оставлял бы администратора править шаблон без всякого эффекта; та же позиция, что уже применена к `pkcs12_token_object_label` на USB. Тесты `a_path_pattern_with_the_token_source_is_refused`, `each_carrier_refuses_the_other_carriers_key`; краснота проверена

- [x] 3.17 (ревью) `strict` + `token_object` отвергается при чтении конфигурации — `validate_pkcs12_source` получает эффективный `monitor.fail_mode`; комбинация целиком известна на этом этапе, и оператор узнаёт о ней, когда пишет конфигурацию. Проверка на пути входа сохранена для конфигураций, собранных в коде, и перенесена в диспетчер `authenticate` — до pre_auth-хуков, рядом с таким же гардом ветки PKCS#11, чтобы хуки с побочными эффектами не отрабатывали на входе, который не мог состояться. Тесты `strict_monitoring_with_the_token_source_is_refused_at_configuration_time`, `strict_monitoring_with_the_usb_carrier_is_untouched`, `strict_monitoring_is_refused_for_the_token_carrier_before_any_hook_runs` (проверяет, что `MockExec` не получил ни одного вызова); краснота проверена по обеим правкам

- [x] 3.9 (сверх плана, нужно для работоспособности 3.4) Демон читает `usb_serial` в том пространстве имён, которое задаёт носитель, а не режим — `credential_mode_for` в `daemon/mod.rs`. При `mode = "pkcs12"` демон сверял serial сессии с блочным устройством в udev: serial токена там не находится, `SessionOpen` отвечал бы `DEVICE_GONE` (фатально в любом fail-mode) и вход не состоялся бы вовсе, а флешка с совпавшим serial могла бы завершить чужую сессию. Тесты `daemon::credential_mode_tests::*`; краснота проверена возвратом прежнего сопоставления по `mode`

## 4. Запись конверта инструментом выпуска

- [x] 4.1 Тест: запись объекта, превышающего лимит устройства, отвергается до обращения к токену — `carrier::tests::an_oversized_container_is_refused_before_the_module_is_touched` (путь модуля заведомо несуществующий: неверный порядок проверок дал бы ошибку модуля), плюс `an_oversized_container_is_refused_with_both_sizes`
- [x] 4.2 Тест: расхождение перечитанного объекта с исходником трактуется как неуспешная запись — `a_truncated_read_back_is_a_failed_write`, `a_read_back_of_the_right_length_but_the_wrong_bytes_is_a_failed_write`, `an_object_that_came_back_public_is_a_failed_write`
- [x] 4.3 Реализовать запись конверта на токен: `CKA_PRIVATE = true`, проверка размера, обязательная верификация чтением — `carrier::lay_out_token` + CLI `prepare-carrier --module/--object-label`; при расхождении объект уничтожается, чтобы фрагмент не остался на носителе
- [x] 4.4 Проверить на живом Рутокен Lite: конверт 2–3 КБ пишется и читается, объект 48 КБ отвергается (а не усекается молча) — `tests/pkcs11_data_object.rs` (5/5) и прогон CLI: 2600 Б записан и прочитан `pkcs11-tool` побайтово идентично, без логина объект не виден, 48 КБ отвергнут до обращения к токену (на токене ничего не появилось)

- [x] 4.5 Замена не уничтожает прежнее удостоверение до того, как новое проверено: запись идёт под меткой `.tessera-staging-<label>`, верифицируется там, переименовывается (`C_SetAttributeValue` на `CKA_LABEL`) и только потом старые объекты удаляются — `carrier::lay_out_token`; живой тест `live_a_failed_write_leaves_the_existing_credential_in_place` (память токена забивается, запись отвергается, прежний конверт читается байт в байт, временной метки на токене не остаётся); юнит-тесты `the_longest_accepted_label_still_fits_once_it_is_staged`, `a_staged_label_is_marked_as_one`
- [x] 4.6 Логин снимается на всех путях выхода и `CKR_USER_ALREADY_LOGGED_IN` не выдаётся за неверный PIN — RAII-обёртка `LoggedInSession` в `carrier.rs` (logout в `Drop`, вытеснение остаточного логина при открытии); живой тест `live_write_succeeds_while_a_neighbour_holds_a_login` (сессия `Pkcs11Session` держит логин на том же `C_Initialize`, запись обязана пройти, и вторая запись в том же процессе тоже)
- [x] 4.7 Провал уборки фрагмента не проглатывается — `CarrierError::TokenFragmentLeft` несёт причину, статус PKCS#11 и требование ручной очистки (`discard_fragment`); юнит-тест `a_leftover_fragment_is_reported_with_its_cause`
- [x] 4.8 PIN токена-носителя берётся из отдельного источника: переменная `TESSERA_CARRIER_PIN` и подпись, называющая токен-получателя (`--token-label` попадает в приглашение) — `pin::CliPinSource::for_carrier`; юнит-тест `a_prompt_names_the_device_it_is_asking_about`, живая проверка CLI: с одной только `TESSERA_ISSUER_PIN` запуск отвергается и называет `TESSERA_CARRIER_PIN`
- [x] 4.9 Читатель не мягче писателя к дубликатам метки — `Pkcs11Error::DataObjectAmbiguous` при нескольких одинаково приватных объектах (публичная приманка по-прежнему проигрывает приватному конверту); живой тест `live_two_objects_under_one_label_are_refused`
- [x] 4.10 Конверт не остаётся в освобождённой памяти: копия в шаблоне `C_CreateObject` затирается после вызова, перечитанное значение возвращается в `Zeroizing` — `carrier::lay_out_token`, `read_back_object`
- [x] 4.11 `Debug` у `FoundDataObject` печатает размер и флаг приватности, но не байты — тип живёт в auth-пути, где `debug!(?found)` высыпал бы PKCS#12 в журнал; юнит-тест `debug_shows_the_size_and_not_the_container`
- [x] 4.12 Флаги, которые умеет только токен (`--pinentry/--pin-stdin/--pin-file/--token-label`), отвергаются на запуске с `--media`, а не молча игнорируются; размер `.p12` сначала проверяется по записи каталога — юнит-тест `a_mounted_carrier_refuses_the_flags_that_belong_to_a_token`

## 5. Документация и границы

- [ ] 5.1 Описать носитель в `docs/ru` и `docs/en`: как записать конверт, требование приватности объекта, лимит размера
- [ ] 5.2 Явно зафиксировать уровень доверия — «съёмный носитель плюс PIN», ключ извлекаем, подпись на хосте; проверить, что формулировок об аппаратной защите ключа нет
- [ ] 5.3 Свериться с маркетинговыми материалами (`docs/marketing/`) — не появилось ли обещаний, которых этот носитель не даёт
- [ ] 5.4 Записать в runbook живой проверки: сценарий пассивного токена и обязательная сверка round-trip
- [ ] 5.5 `master-code-reviewer` по всему диффу; CRITICAL/HIGH закрыть до мержа
