## 1. Вытеснение и живые сессии

- [ ] 1.1 Тест: при живой залогиненной сессии вторая аутентификация в том же процессе получает диагностируемый отказ, а логин первой остаётся действительным
- [ ] 1.2 Тест: при отсутствии живых сессий вытеснение работает как прежде (регрессия на поведение родительского change'а)
- [ ] 1.3 Процесс-глобальный счётчик живых залогиненных сессий; инкремент при успешном `C_Login`, декремент в `Drop`
- [ ] 1.4 Отдельный вариант ошибки для «логин занят живой сессией» + маппинг в PAM-код

## 2. Диагностика

- [ ] 2.1 Логировать исход вытеснения, включая `CKR_USER_ALREADY_LOGGED_IN` на повторном логине — сейчас пишется только начало
- [ ] 2.2 Снять WARN с каждого вытеснения в устоявшемся состоянии, оставив запись об аномалии
- [ ] 2.3 Привести комментарий про ретроактивность апгрейда режима к фактической гарантии («начиная со следующего вызова»)
- [ ] 2.4 Убрать избыточное `adopted &&` в условии логирования конфликта режимов

## 3. Маппинг средовых отказов

- [ ] 3.1 Тест: `LogoutFailed` из `open` даёт `PAM_AUTHINFO_UNAVAIL` (9)
- [ ] 3.2 Перенести `LogoutFailed` в класс средовых отказов в матрице `FlowError` → PAM-код
- [ ] 3.3 Проверить экранную диагностику: сообщение не должно говорить об ошибке ввода

## 4. Тестовый харнесс

- [ ] 4.1 `fresh_module_path`: запрет копирования по признаку провайдера (opt-out), а не тотальный — сейчас три теста выключены на однопровайдерном хосте, включая единственную проверку счётчика присвоений и форсирования `Mutex`
- [ ] 4.2 `residual_login_with_wrong_pin_is_rejected`: сделать восстановление попытки PIN надёжным либо явно ограничить прогон токенами с достаточным бюджетом

## 5. Приёмка

- [ ] 5.1 Прогон на живом Рутокене с `PKCS11_MODULE_PATH_2` и записанным приватным объектом
- [ ] 5.2 `master-code-reviewer`; CRITICAL/HIGH закрыть до мержа
- [ ] 5.3 Запись в changelog: параллельная аутентификация теперь отказывает внятно вместо взаимной поломки
