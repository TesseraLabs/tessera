# Tasks: windows-cp

## 1. Walking skeleton

- [ ] 1.1 Прототип тайла CP V2 на Rust (`windows`-крейт, COM-обвязка `ICredentialProvider`/`ICredentialProviderCredential2`): статические поля, регистрация CLSID, тайл виден в LogonUI на стенде; при граблях COM-на-Rust — решение о C++-шелле (design D1) фиксируется здесь
- [ ] 1.2 Panic-guard на всех COM-точках входа + деградация тайла в состояние ошибки

## 2. Поля и обмен со службой

- [ ] 2.1 Клиент named pipe в CP (NDJSON, строгая версия, bounded read) поверх транспортной абстракции `tessera_proto`
- [ ] 2.2 Комбобокс ролей: запрос перечня у службы, порядок по `level`, без дефолта; обновление через `ICredentialProviderEvents`
- [ ] 2.3 Комбобокс метода (серт-носитель активен, «код» зарезервирован/неактивен), PIN-поле с зануление после передачи, статусное поле
- [ ] 2.4 Флоу аутентификации: сбор материала носителя → запрос службе → обработка вердикта; отказ/недоступность = «нет учётных данных» + статус

## 3. Сериализация

- [ ] 3.1 `GetSerialization` → `KERB_INTERACTIVE_UNLOCK_LOGON` технической УЗ строго после вердикта; одноразовый секрет из вердикта, zeroize после употребления (при выборе AP-варианта в spike `windows-tcb-service` — сериализация пакета для AP, тот же контракт)
- [ ] 3.2 Негативные ветки: отказ, обрыв pipe посреди обмена, несовпадение версии — без сериализации, fail-closed

## 4. Дистрибуция

- [ ] 4.1 MSI (WiX): служба + CP DLL + CLI, регистрация CLSID, post-install `prepare`, uninstall с дерегистрацией и сохранением журнала
- [ ] 4.2 Authenticode-подпись DLL/EXE/MSI в релизном pipeline; сборка без подписи не публикуется
- [ ] 4.3 CI: сборка DLL и MSI в Windows-джобе

## 5. Верификация

- [ ] 5.1 Unit-тесты не-COM логики (состояние полей, протокольный клиент, обработка вердиктов, zeroize)
- [ ] 5.2 E2E на Windows-стенде: установка MSI → вход по носителю с выбором роли → группы/integrity в токене → деинсталляция возвращает штатный вход
- [ ] 5.3 Отказоустойчивость: убить службу при открытом LogonUI — тайл в ошибке, стандартный вход жив; краш-инъекция в CP — LogonUI живёт
- [ ] 5.4 E2E на domain-joined стенде: тайл и вход работают идентично вне-доменному устройству; GPO-исключение CP → тайл исчезает, `doctor` диагностирует
- [ ] 5.5 Полный тест-сьют + clippy; sync спеки capability в main после реализации
