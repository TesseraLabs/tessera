# Tasks: role-account-login

- [x] 1. `role_selection.rs`: `parse_user_role` заменяется выводом роли из имени учётной записи; невалидный `role_id` — отказ с внятной причиной. Таблица edge-случаев суффикса заменяется таблицей допустимых/недопустимых имён УЗ
- [x] 2. `entry.rs`: удалить `prompt_for_role`, разбор суффикса и `pam_set_item(PAM_USER)`; роль берётся из прочитанного `PAM_USER`
- [x] 3. `pam_helpers.rs`: удалить помощник канонизации `PAM_USER`, если после (2) он не используется
- [x] 4. `flow.rs`: привести поля и комментарии `RoleStage` к тому, что роль равна имени УЗ входа
- [x] 5. Проверить, что `verify_user_binding` и покрытие `allowed_roles` остались раздельными проверками одного имени (design Решение 4) — слить их нельзя
- [x] 6. Тесты: вход в валидную ролевую УЗ; отказ при имени не-`role_id`; отказ при УЗ вне `user_binding`; отказ при роли вне `allowed_roles`. Тесты разбора суффикса удалить, а не переписать под новое поведение
- [x] 7. Убедиться, что ни один путь больше не вызывает `pam_set_item(PAM_USER)` в аутентификации (grep + чтение)
- [ ] 8. `openspec/specs/role-selection/spec.md` — синхронизация при архивации. ВАЖНО: `roles-always-enforced` тоже правит требование «Явный выбор роли, без дефолта»; архивировать его РАНЬШЕ этого изменения, иначе его правка потеряется
- [x] 9. `docs/{ru,en}/`: везде, где вход описан суффиксом или prompt'ом (`architecture.md`, `role-selection`-разделы, руководства) — переписать на вход в ролевую УЗ
- [x] 10. `docs/ru/changelog.md` — Breaking, с миграцией
- [ ] 11. Прогон e2e (`--profile ubuntu-container --filter roles` и `--filter issuer`): `ROLE-001…004` и `ISS-005` должны стать `PASS`; обновить `BASELINE.md`
